# AICoding 场景驱动实战 Prompt

> 适用于限时 AICoding 笔试：题目提供 README、现有业务代码和可编辑测试脚本，最终评测由考生手动运行，并只返回业务场景名称或简短反馈。
>
> 本文来自第三次真实笔试复盘。它不是通用软件工程流程，也不声称能够还原隐藏测试源码；目标是把当前已知业务要求转成可实现、可本地验证、可根据平台反馈继续排查的业务链。

## 1. 先理解这套 Prompt 在解决什么

整套流程只维护两份辅助文档：

```text
spec.md：已知业务目标、Task、对应测试和已经确认的实现事实
debug.md：顽固场景的业务链、诊断证据、规则歧义和手动测试结果
```

README、业务代码和原测试脚本仍然是实现时必须读取的材料，不应被完整复制进 `spec.md`。

```text
README + 首次最终反馈
        ↓ Prompt 1
当前已知的完整业务场景
        ↓ Prompt 2
场景—Task—验收样例映射
        ↓ Prompt 3（每个 Task 使用一个实现会话）
业务实现 + 原测试脚本中的对应测试
        ↓ 考生手动运行
本地结果 / 最终平台反馈
        ↓ Prompt 4（仅在顽固场景出现时）
完整业务链定位、修复和证据记录
```

这套方法不再使用 `AICODING_STATE.md`、P0、固定切片体系、严格 TDD、停止条件或 Git 发布流程。限时做题时，只有直接帮助理解业务、实现代码和定位失败的内容值得保留。

## 2. 使用前准备

把 Prompt 中的占位符替换为实际内容：

- `@README.md`：题目需求源文件；
- `@全部业务代码`：本题当前的业务实现文件；
- `@原测试脚本`：平台提供且允许编辑的测试脚本；
- `{x}`：开始实现前第一次运行最终评测得到的全部业务场景反馈；
- `Task{x}`：本轮只实现的 Task 编号；
- `{y}`：某一个仍未通过的最终评测场景；
- `{z}`：考生手动运行本地测试或最终评测后复制的真实输出。

Agent 无法读取评测页面时，由考生负责两次信息传递：

1. 手动运行测试；
2. 把完整命令、关键输出和业务场景反馈原样粘贴回会话。

最终评测反馈只能证明平台关注某个业务结果，不能证明隐藏测试使用了什么输入和断言。不要把场景名称直接当成隐藏测试规格。

每个 Prompt 都提供完整版本和精简版本：时间允许时优先使用完整版本；时间紧张时只复制对应的精简版本，不需要把两个版本同时发送。精简版本只压缩解释和格式，不改变该阶段的任务边界。

## 3. Prompt 1：建立当前已知的完整业务目标

### 什么时候使用

进入项目后先手动运行一次最终评测，复制平台显示的全部业务场景，随后在第一个会话使用本 Prompt。

本阶段只建立 `spec.md` 的业务目标，不拆 Task，也不实现业务代码。

```text
【请完成项目业务理解并创建 spec.md，本轮不实现业务代码】

【执行】

1. 阅读需求源文件@xx、项目测试样例脚本@xx、现有业务代码@xx，以及最终测试反馈{x}。
2. 先逐条分析最终测试反馈，再结合需求整理当前已知的完整业务场景目标；每条反馈都必须被某个场景明确覆盖，同义场景可以合并，但不能被笼统的主流程概括掉。
3. 创建 spec.md，按业务场景写清触发条件、操作过程和最终可观察结果，包括响应、主对象状态、关联对象变化及失败时不得改变的内容。
4. 最终反馈只用于确定需要覆盖的业务目标；题面未明确且存在多种合理实现的规则，记录具体歧义，不要自行选择。本轮先不拆 Task 或编写实现方案。
```

### 时间不足时精简版

```text
读取 @需求、@测试脚本、@现有代码和最终测试反馈{x}，创建 spec.md，本轮不实现业务代码。逐条吸收最终反馈并结合需求整理当前已知的完整业务场景，每条反馈都要有明确归属，不能被笼统主流程代替。写清各场景的触发条件、操作过程、最终响应、状态联动及失败时不得改变的内容；存在多种合理解释时记录歧义，不自行定案，先不拆 Task。
```

### 使用辅助

- “当前已知的完整”只表示覆盖 README 和本次已知反馈，不表示已经知道全部隐藏测试。
- 不要要求“整理核心业务”或“记录 P0”。这会诱导 Agent 只保留主流程，把异常、幂等、结算和跨实体联动归入笼统章节。
- 两条反馈如果会产生不同响应、状态或副作用，应保留为不同业务场景；只有触发条件和全部结果相同，才适合合并。
- `spec.md` 第一版关注“最终应该发生什么”。文件、函数和具体算法留到 Task 划分与实现阶段。

## 4. Prompt 2：核对规格并划分可验收 Task

### 什么时候使用

`spec.md` 已包含当前已知业务场景后，使用新的会话执行本 Prompt。它会先反向核对第一份文档，再建立场景、Task 与验收样例之间的对应关系。

```text
【执行任务划分】

【执行】

1. 读取 @README.md、@spec.md、@当前业务代码、@原测试脚本和最终测试反馈{x}。不要默认 spec.md 正确：先逐条核对 README 与最终反馈中的已知场景是否全部进入 spec.md，并检查其中是否存在遗漏、冲突或被擅自确定的歧义；发现问题先修正 spec.md。
2. 把 spec.md 中的业务场景划分为可执行、可验收的 Task。每个 Task 应形成一条从真实输入、公开接口调用到最终可观察结果的完整业务路径，并写明对应的 spec 场景与最终测试反馈、业务结果和当前代码缺口。
3. 为每个 Task 设计一个可直接加入原测试脚本的验收样例，写清数据准备、调用顺序，以及对响应、主对象状态、关联对象变化和不得变化内容的断言。仍有多种合理解释的规则继续标明歧义，不要在任务划分时自行决定。
```

### 时间不足时精简版

```text
读取 @README.md、@spec.md、@当前业务代码、@原测试脚本和最终测试反馈{x}，执行任务划分。先反向核对 README 和每条最终反馈是否都被 spec.md 覆盖，并修正其中的遗漏、冲突和被擅自确定的歧义。再把业务场景划成从真实输入到可观察结果的完整 Task，写明对应场景与反馈、业务结果、当前代码缺口，以及可直接加入原测试脚本的数据、调用和断言。
```

### 使用辅助

“重新核对”必须是双向的：

```text
README / 最终反馈 → spec.md：检查是否遗漏
spec.md → README / 最终反馈：检查是否冲突或无依据
```

Task 按完整业务结果划分，而不是按接口或文件划分。多个接口共同产生一个可观察结果时，应属于同一 Task；使用同一接口但产生不同副作用的场景，可以属于不同 Task。

本阶段只设计验收样例，不需要一次性把所有测试写入脚本。验收样例会在对应 Task 的实现会话中落地。

## 5. Prompt 3：在完整业务链中快速实现一个 Task

### 什么时候使用

为每个 Task 单独打开实现会话。每个会话读取最新代码、`spec.md` 和原测试脚本，但只实现一个指定 Task。

```text
【请完成 Task{x} 的快速实现】

读取 @README.md、@spec.md、@全部业务代码和 @原测试脚本，按照 spec.md 实现 Task{x}，并将该 Task 已规划的验收样例加入原测试脚本。

实现时重点检查它在完整业务链中的衔接：上游 Task 产生的状态和数据、当前 Task 修改的共享状态、下游 Task 依赖的结果，以及跨 Task 共用的失败不变、幂等和时间规则。验收样例应尽量通过真实调用链到达目标状态，不能只直接构造中间状态。

本轮只修改业务代码和测试脚本，不更新文档，也不记录未经手动运行的测试结果。
```

### 时间不足时精简版

```text
读取 @README.md、@spec.md、@全部业务代码和 @原测试脚本，快速实现 Task{x}，并把已规划的验收样例加入原测试脚本。不要把该 Task 当成孤立功能，重点核对上游状态、共享数据修改、下游结果，以及跨 Task 的失败不变、幂等和时间规则；测试应尽量通过真实调用链到达目标状态。本轮不更新文档或预写测试结果。
```

### 使用辅助

- Prompt 2 已经定义 Task 的业务结果和测试样例，Prompt 3 不需要重新设计任务。
- 实现会话仍需读取 README，因为 `spec.md` 不会转录全部固定接口、字段和状态。
- “检查其他 Task”不代表顺便实现其他 Task，而是核对上游输入、共享数据和下游依赖。
- 测试尽量使用公开接口到达目标状态。直接伪造“派送中”“已结算”等中间状态，可能掩盖上游 Task 与当前 Task 的衔接错误。
- 实现完成后由考生手动运行新增测试和现有测试。普通语法、导入或断言错误可以直接粘贴回原实现会话纠正，不需要额外 Prompt。

## 6. 手动运行后：更新 Task 的事实记录

Agent 无法读取考生终端或最终评测页面，因此文档更新必须发生在考生提供结果之后。

```text
【更新 Task{x} 的实现记录】

读取 @spec.md、@业务代码和 @原测试脚本，根据我手动提供的测试结果{z}，在 Task{x} 下记录实际修改位置、对应测试及此次本地运行结果。只记录代码、测试和结果能够直接证明的事实；本地测试通过不等于最终评测通过，没有提供最终平台反馈时不得写为最终通过。
```

### 时间不足时精简版

```text
根据我手动提供的 Task{x} 测试结果{z}，核对 @业务代码和 @原测试脚本，并更新 @spec.md 中该 Task 的实现记录，只写实际修改、对应测试和本次本地结果。不要推断未展示的测试结果，也不要把本地通过写成最终评测通过。
```

建议只记录：

```text
实现位置：
对应测试：
本地结果：
最终评测：未验证 / 用户提供的实际结果
```

不要把每次语法错误和简单修正都写入文档。它们属于实现过程，不是值得跨会话保留的业务证据。

## 7. Prompt 4：顽固场景排查

只有最终评测仍出现复杂业务场景失败时，才使用本节。不要用一条宽泛 Prompt 同时猜规则、改代码和宣布通过；根据当前证据选择对应模块。

### 路由表

| 当前情况 | 使用的 Prompt |
|---|---|
| 刚取得一个新的最终失败场景 | Prompt 4A：定位完整业务链 |
| 还没有覆盖该场景的本地全链路测试 | Prompt 4B：建立诊断测试 |
| 手动运行本地测试后出现业务断言错误 | Prompt 4C：定位第一个错误边界 |
| 本地全链路通过，但平台场景仍失败 | Prompt 4D：排查业务规则歧义 |
| 已根据本地结果确认根因 | Prompt 4E：实施通用修复 |
| 完成手动复测，需要保存结论 | Prompt 4F：记录真实结果 |

基础语法或测试脚本错误不需要进入整套排查，直接在当前会话根据终端输出修正即可。

### Prompt 4A：定位失败场景的完整业务链

```text
【定位最终评测失败场景】

读取 @README.md、@spec.md、@全部业务代码、@原测试脚本和 @debug.md，分析我手动提供的最终评测反馈{y}，本轮只处理其中一个失败场景。

从 spec.md 找出该场景对应的全部 Task，并按真实执行顺序还原完整业务链。逐段核对每个 Task 接收的上游状态、产生的下游状态、修改的共享数据，以及共同使用的失败不变、幂等、逻辑时间和 reset 规则；同时检查现有测试覆盖了哪些环节、遗漏了哪些 Task 边界。

将场景、Task 链、已有测试和第一个尚未验证或存在冲突的环节记录到 debug.md。本轮不修改业务代码，也不根据场景名称猜测隐藏输入。
```

#### 时间不足时精简版

```text
读取 @README.md、@spec.md、@业务代码、@原测试脚本和 @debug.md，针对最终失败场景{y}还原完整 Task 链，核对上下游状态、共享数据、共同规则和现有测试，找出第一个未验证或冲突环节并记入 debug.md。本轮不改代码，不猜隐藏输入。
```

目标不是罗列所有可能原因，而是找出完整业务链中第一个缺少证据的环节。

### Prompt 4B：建立全链路诊断测试

```text
【为失败场景建立全链路诊断测试】

读取 @README.md、@spec.md、@全部业务代码、@原测试脚本和 @debug.md，针对失败场景{y}，把 debug.md 中记录的完整 Task 链写成一个本地诊断测试。

使用原测试脚本已有的初始化和调用方式，尽量通过公开接口依次完成真实业务流程，不要直接伪造目标中间状态。在每个 Task 边界分别断言主对象状态、关联对象状态和共享数据；失败路径还要保存操作前状态，并断言不应变化的数据。

只修改原测试脚本，不修改业务代码。在 debug.md 中记录新增测试名称和用户需要手动执行的准确命令，不填写未经用户提供的运行结果。
```

#### 时间不足时精简版

```text
读取 @README.md、@spec.md、@全部业务代码、@原测试脚本和 @debug.md，按失败场景{y}的 Task 链新增一个经公开接口运行的全链路测试；逐个 Task 边界断言主对象、关联对象、共享数据及失败不变项。只改测试，并在 debug.md 记录测试名和手动命令，不填写结果。
```

该测试的价值是让一次失败直接显示业务链在哪个 Task 边界首次偏离预期。

### Prompt 4C：定位第一个业务错误

```text
【定位全链路中的第一个业务错误】

读取 @README.md、@spec.md、@全部业务代码、@原测试脚本和 @debug.md，并分析我手动提供的测试结果{z}。

找到完整业务链中第一个失败的断言或异常位置，不要从最后的失败结果直接猜测根因。比较该位置上游 Task 应产生的状态与下游 Task 实际读取的状态，并检查相关共享字段的全部读取和写入位置。

依次判断问题属于：状态未传递、共享数据漏更新或重复更新、执行顺序错误、失败后留下部分修改、重复调用不幂等、逻辑时间使用错误，还是 reset 残留。把第一个真实偏差、相关 Task 和对应代码位置记录到 debug.md。本轮先不修改业务代码。
```

#### 时间不足时精简版

```text
读取 @README.md、@spec.md、@全部业务代码、@原测试脚本和 @debug.md，根据全链路测试结果{z}定位第一个失败断言，比较上游输出与下游输入并检查共享字段全部读写。判断属于状态、重复更新、顺序、原子性、幂等、时间或 reset 问题，把首个偏差和代码位置记入 debug.md，本轮不改代码。
```

最终断言失败可能只是上游错误的连锁结果，因此应从第一个偏差开始定位。

### Prompt 4D：本地全链路通过时排查业务歧义

```text
【排查失败场景中的业务规则歧义】

读取 @README.md、@spec.md、@全部业务代码、@原测试脚本和 @debug.md。当前失败场景{y}的本地全链路测试已经通过，但最终平台评测仍未通过。

先区分该场景中由 README 明确规定的规则与当前实现自行推导的规则。每次只选择一个会改变最终可观察结果、但题面没有唯一说明的规则，写出两种有依据的解释 A/B，以及它们会在哪个输入条件、状态变化或最终结果上产生差异。

在原测试脚本中加入一组只改变该条件的对照测试，不同时改变其他规则，也不修改业务代码。将 A/B 的依据、测试名称和用户需要手动执行的命令记录到 debug.md，不预先选择正确解释。
```

#### 时间不足时精简版

```text
本地全链路已通过但最终场景{y}仍失败。读取 @README.md、@spec.md、@业务代码、@原测试脚本和 @debug.md，区分明确规则与实现推导；只选一个会改变结果的未定规则，写出 A/B 解释并加入只改变该条件的对照测试。只改测试和 debug.md，不改业务代码或预选答案。
```

本地对照测试不能证明平台选择 A 还是 B。它的作用是让下一次代码调整和平台反馈具有可解释性，而不是围绕同一个猜测增加相似样例。

### Prompt 4E：修复已经定位的业务规则

```text
【修复已定位的业务规则】

读取 @README.md、@spec.md、@全部业务代码、@原测试脚本和 @debug.md，根据我提供的测试结果{z}，修复其中已经定位的第一个业务偏差。

修改产生该状态或共享数据的实际责任位置，并核对读取同一状态的相关 Task，避免只在最后失败接口中增加特判。共享数据必须在正确阶段更新且只更新一次；失败路径不得留下部分修改，重复调用不得重复产生副作用。

保留现有公开接口和已通过业务规则，在原测试脚本中补充能够覆盖本次根因的全链路回归断言。本轮不更新结果状态，也不假定修复已经通过。
```

#### 时间不足时精简版

```text
读取 @README.md、@spec.md、@全部业务代码、@原测试脚本和 @debug.md，根据已确认结果{z}修复第一个业务偏差的责任位置，并核对读取同一状态的相关 Task；共享数据只更新一次，失败不留部分修改，重复调用不重复产生副作用。保留公开接口并补全链路回归断言，不更新结果或预写通过。
```

修复应落在第一个错误状态的产生位置，而不是在最终报错接口中为当前样例打补丁。

### Prompt 4F：记录手动测试或最终评测结果

```text
【记录本轮手动测试结果】

读取 @spec.md、@debug.md、@业务代码和 @原测试脚本，根据我手动提供的测试结果{z}更新 debug.md。

写明本次运行的是本地 Task 测试、本地全链路测试还是最终平台评测，并记录对应场景、Task 链、测试名称、实际通过或失败结果、已确认根因、实际代码修改和仍未验证的问题。只记录结果能够直接证明的事实。

只有最终平台反馈明确证明场景已经通过时，才把已经确认的业务规则同步回 spec.md；本地测试通过不能写成最终评测通过。
```

#### 时间不足时精简版

```text
读取 @spec.md、@debug.md、@业务代码和 @原测试脚本，根据我手动提供的结果{z}更新 debug.md，写明结果类型、场景、Task 链、测试、通过或失败、根因、修改和待验证项，只记录直接证据。只有最终平台反馈确认通过时才同步 spec.md；本地通过不等于最终通过。
```

## 8. 顽固场景的推荐使用顺序

如果本地还没有完整复现：

```text
4A 定位 Task 链
→ 4B 添加全链路诊断测试
→ 考生手动运行
→ 4C 定位第一个错误边界
→ 4E 修复
→ 考生手动复测
→ 4F 记录
```

如果本地全链路完全通过，但最终平台仍报告同一场景：

```text
4A 定位 Task 链
→ 4B 确认本地链路通过
→ 4D 每次调查一个歧义规则
→ 考生手动运行对照测试
→ 4E 根据证据修复
→ 考生重新运行最终评测
→ 4F 记录
```

## 9. 常见误区

### 把最终场景名称当成隐藏测试

平台只公开业务结果时，不能据此推断准确输入、阈值和断言。应先验证已知业务链，再调查一个具体歧义。

### 单 Task 测试直接构造中间状态

这种测试可能证明最后一个接口可运行，却无法证明上游 Task 能产生兼容数据。复杂场景应增加真实调用链测试。

### 最后一个接口报错，就只修最后一个接口

完整业务测试的最终失败可能来自更早的状态、计数或副作用。应定位第一个错误边界。

### 代码和测试共享同一个错误假设

由同一规则解释生成的代码和本地测试可能同时通过。只有本地全链路已通过、平台仍失败时，才进入 A/B 歧义排查。

### 把“已修改”写成“已通过”

代码存在、本地测试通过和最终平台验收通过是三个不同事实。只有考生提供的实际结果才能更新相应状态。

## 10. 最小记忆

```text
先用最终反馈补全业务目标，
再让场景、Task 和测试一一可追踪；
每个 Task 都在完整业务链中实现，
复杂失败先找第一个错误边界，
本地全链路通过后才调查规则歧义。
```
