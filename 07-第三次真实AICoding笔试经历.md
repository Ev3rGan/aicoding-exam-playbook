# 第三次真实 AICoding 笔试经历：场景驱动实现

> 状态：经历记录 v0.1
>
> 用途：记录目前最接近成功的一次做题路径，并修正前两版方法中过度工程化的部分。
>
> 证据边界：关于常见题目结构和隐藏验收机制的内容来自个人经历与推断，不代表所有平台的公开规则。

## 1. README 与固定接口

较完整的题面可能包含背景、系统要求、业务流程、状态枚举、实体字段、业务约束、场景提示、逻辑时间和后端接口。截图中的快递履约题属于信息非常充分的示例，可以让 Agent 直接提取大量实现约束。

更多题目的 README 只有简要业务要求，需要根据接口、原始代码和测试自行补全实现方式。相比一般业务描述，后端接口通常更加固定，可能是平台统一调用和验收程序的入口。因此方法、路径、字段位置和响应结构是实现时必须优先保持的边界。

## 2. 旧 Prompt 的主要偏差

之前的 Prompt 把真实工程中的状态管理、Task 切分、TDD、停止条件和多会话治理带入了笔试，但这些内容并没有直接提升业务通过率。

实际使用中，Agent 可以理解“根据报错信息进行修复”这种普通目标描述。大量统一字段反而可能增加阅读负担。`AICODING_STATE.md`、每个 Task 一个测试以及固定 red-green 流程也不一定产生新的业务信息。

更关键的问题是：只让 Agent “读取 README 并生成技术文档”，得到的往往仍是抽象的功能说明。文档没有明确接口处理步骤、状态变化、跨实体副作用、代码函数和测试输入，Agent 实现时仍需重新猜测业务。

## 3. 公开测试脚本的作用

多数经历中，题目会提供一个可以编辑的原始测试脚本。它不仅用于检查代码，也暴露了：

- 应用怎样启动和重置；
- 测试客户端怎样调用接口；
- 请求和响应字段怎样组织；
- 一个业务场景怎样准备数据和进行断言。

平台最终运行按钮可能对应一套覆盖更广的类似测试，但通常只返回未通过的业务场景名称或简要描述，而不公开具体输入和断言。这是一种经验推断，但它给出了有效做题方向：把公开测试扩展为本地可见的“隐藏场景近似集”。

## 4. 最接近成功的做题方式

本次更有效的路径是：

1. 在项目开始时整理全部已知业务场景；
2. 根据 README、固定接口、原始代码和测试脚本生成具体到代码步骤的 `spec.md`；
3. 在原测试脚本中为这些场景补充输入、接口调用和断言；
4. 根据测试失败反推业务代码，而不是先凭抽象功能清单完成全部实现；
5. 平台报告某类场景失败后，将该描述展开成一组本地测试样例；
6. 根据新测试定位规则缺口，修改代码并同时回归新增测试和完整测试；
7. 用一份简短纠错文档保存业务场景、测试、根因、修改和结果。

这里的测试不是“每个 Task 一个测试”的工程仪式，而是把业务自然语言转换成可执行实例，并帮助区分不同实现是否真的符合规则。

## 5. 对后续 Prompt 的直接要求

### 5.1 技术文档必须回答“代码怎样写”

Prompt 不能只要求总体方案、模块、接口和测试策略，还要指导 Agent 展开：

- 每个接口的请求、响应、校验与副作用；
- 每个实体的字段、默认值和状态转换；
- 每个业务动作的准确执行顺序；
- 多个实体之间怎样联动和回滚；
- 逻辑时间、容量、额度、金额和重复调用怎样处理；
- 这些逻辑分别落在哪些文件、函数和调用链；
- 哪些场景能够验证每条重要规则。

### 5.2 纠错必须回答“怎样从场景造出测试”

平台只给出简要业务场景时，Prompt 应指导 Agent：

1. 还原前置数据；
2. 设计公开接口调用序列；
3. 同时断言响应与后续状态；
4. 补充正常、边界、非法状态、重复调用和跨实体副作用；
5. 先运行测试获得真实差异，再修改实现；
6. 重跑新增测试和完整测试，避免只对一个样例特判。

## 6. 当前结论

目前更值得保留的不是复杂流程，而是两个工作文档：

- `spec.md`：将不完整题面翻译成可直接实现的代码级设计；
- `debug.md`：将平台场景翻译成测试，再记录真实失败、原因、修改和结果。

旧版 Prompt 是否删除仍可在新版本经过实际笔试验证后决定。当前更合理的做法是先保留为历史对照，不继续把它们作为默认推荐方案。
