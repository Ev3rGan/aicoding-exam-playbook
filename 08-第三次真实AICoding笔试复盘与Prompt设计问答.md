# 第三次真实 AICoding 笔试复盘与 Prompt 设计问答

> 本文以“经历者提问—分析者回答”的形式，整理第三次真实 AICoding 笔试后的连续复盘。重点不是展示一份凭空设计的方法论，而是保留问题怎样从真实执行、测试反馈和多会话协作中逐步暴露，以及 Prompt 为什么据此修改。
>
> 本文只讨论第三次经历。前两次笔试、旧版 P0/切片体系、`AICODING_STATE.md`、严格 TDD、停止条件和 Git 发布流程不构成本次方法的依据。

## 一、这次经历真正改变了什么

### 经历者

这次题目提供了 README、现有业务代码和可以编辑的测试样例脚本。最终评测需要我手动运行，Agent 无法直接看到评测页面，我只能把平台显示的业务场景复制到聊天中。

我原先采用的是非常简单的 Prompt：先让 Agent 阅读材料，创建 `AICODING_STATE.md`，记录 P0 业务和关键输入输出；然后再划分 Task，切换多个会话分别实现。大多数冒烟、基础和进阶测试最终能够通过，但仍有两三个完整业务场景始终失败。

这次经历应该怎样理解？

### 分析者

这是一次“最接近成功”但尚未完成全部最终验收的真实实践。有效部分并不是复杂的工程流程，而是下面几个事实：

- README 提供题目事实和固定接口；
- 用户可以在实现前手动运行最终评测，把 Agent 看不到的业务场景复制回来；
- 原测试脚本不仅用于检查语法，还展示应用怎样初始化、怎样调用接口以及怎样断言结果；
- 测试可以把自然语言业务要求变成可执行实例；
- 最终失败场景需要回到完整业务链，而不是只修最后报错的接口。

截图中的快递履约题已经给出大量业务事实：包裹、运单、网点、快递员、客户、理赔和 COD 具有各自状态；非法阶段操作不能改变状态；签收、拒收、退回、赔付和结算会产生跨实体副作用；金额使用整数分；时间判断使用逻辑时钟；终态操作和重复调用需要一致处理。

因此，主要困难不是重新总结题面，而是把这些要求转换成：

```text
什么前置状态
→ 经过什么公开操作
→ 当前响应是什么
→ 主对象如何变化
→ 关联对象如何变化
→ 哪些数据不能变化
→ 后续查询应该看到什么
```

这也是为什么 `spec.md` 和测试脚本比新的状态管理体系更重要。

## 二、顽固场景究竟是业务歧义，还是文档导致的臆想

### 经历者

我怀疑最后几个场景可能存在两类原因：

1. 业务逻辑本身有歧义，逻辑 A 和逻辑 B 都说得通，因此无法构造符合真实测试的实现；
2. 初版 `AICODING_STATE.md` 没有标准的文档构筑方式，实现会话在空白处自行臆想了关键逻辑。

哪一种更可能？

### 分析者

它们不是互斥选项，更可能是一条连续因果链：

```text
题目存在部分未写死的业务规则
→ 状态文档只记录 P0 和输入输出，没有暴露歧义
→ 实现 Agent 把一种合理解释当成确定事实
→ 本地测试又按照同一种解释编写
→ 代码和测试互相证明，但最终评测仍不通过
```

“文档没有区分确定规则与待判定规则”是更基础的问题；题目本身的歧义，则可能成为某些隐藏场景的直接触发点。

### 明确规则与真实歧义需要分开

题面已经明确说明的规则，例如：

- 非法阶段操作应拒绝且不改变状态；
- 拒收包裹应退回寄件人；
- COD 只有成功签收才收款，拒收不收款；
- 终态拒绝后续操作，重复签收应幂等；
- 金额使用整数分；
- 时间判断必须使用逻辑时钟。

如果这些场景失败，通常不能归因于业务歧义。更可能是规格遗漏、实现偏移、关联副作用没有完成，或者测试只检查响应而没有检查后续状态。

另一类描述确实可能存在多种合理解释，例如：

- 多个快递员同时可用时，怎样才算合理派单；
- “负载”只统计正在派送，还是包含已经分配但尚未完成的包裹；
- 加急件和高信用寄件人如何影响优先级；
- 网点达到什么条件才算拥堵；
- 多次派送失败或滞留多久后触发退回、改派或重路由；
- 多个候选条件相同时怎样打破平局。

这种情况下，Agent 不应该默默选择一种行为并把它写成事实。`spec.md` 必须把会改变最终结果的 A/B 解释暴露出来。

## 三、为什么第一份 Prompt 需要从“P0”改成“完整业务目标”

### 经历者

我真实使用的第一份 Prompt 非常简单：

```text
请完成项目理解和状态文件 AICODING_STATE.md 建档，本轮不实现业务代码逻辑。

1. 阅读需求源文件、项目测试样例脚本、现有业务实现代码和最终测试反馈。
2. 创建状态文档 AICODING_STATE.md。
3. 记录 P0 业务、关键输入输出。
```

我希望把状态文档改为 `spec.md`。第一步不应直接写具体 Task 的实现方式，而应先把最终业务目标弄清楚。

### 分析者

原 Prompt 的优点是短，而且已经被真实使用证明 Agent 能够执行。问题不在于缺少复杂规范，而在于“P0 业务、关键输入输出”的粒度不足以描述有状态业务。

“P0”只能表达优先级，不能表达判题目标。它没有说明：

- 场景在什么状态下触发；
- 需要连续调用哪些公开操作；
- 成功后哪些对象发生变化；
- 失败后哪些对象必须保持不变；
- 后续查询应该看到什么结果。

“输入输出”同样不足。例如签收接口表面上只有包裹 ID 和成功响应，但最终评测还可能检查包裹状态、运单路段、快递员占用、COD 收款、寄件人余额和重复调用是否重复结算。

因此，第一版 `spec.md` 应先记录业务场景的行为合同，而不是实现方案。每个场景只需要回答：

1. 什么情况下触发；
2. 经历什么业务操作；
3. 当前响应和最终可见结果是什么；
4. 哪些规则明确，哪些规则仍存在歧义。

文件、函数和具体实现步骤留到 Task 划分之后。一个场景只有在触发条件、操作过程和结果已经能够被明确说明时，才适合交给实现会话。

## 四、为什么初版业务场景没有覆盖最终反馈

### 经历者

采用原始 Prompt 创建文档时，我发现 Agent 没有有效利用最终测试反馈。它给出的初版核心业务场景没有完整覆盖反馈。后续很多场景都是先完成基础 P0 后，再根据平台反馈逐个追加，而不是一开始形成相对完整的业务目标。

是否还有优化空间？

### 分析者

有，而且这很可能是后续不断补场景的直接原因。

问题不只是“P0”这个词，而是它会诱导 Agent 进行重要性筛选和抽象合并：优先保留主流程，把最终反馈中的异常、幂等、边界和跨实体联动归入几个上位功能。

例如平台可能分别反馈：

- 收件人拒收后退回；
- COD 拒收时不收款；
- 成功签收后向寄件人结算；
- 重复签收不能重复结算。

Agent 可能把它们概括成“完成签收和异常处理”。概括本身没有错，但无法保证四种可观察结果都进入实现和测试。

因此，最终反馈不能只是参考材料，而应成为必须逐条吸收的已知验收目标。第一步应形成：

```text
README 中明确要求的业务场景
+ 最终测试反馈点名的业务场景
= 当前材料中已知的完整业务目标
```

这里的“完整”只表示完整覆盖当前材料，不代表已经知道全部隐藏测试。

每条反馈都必须在 `spec.md` 中找到明确归属。同义反馈可以合并，但不能被笼统的主流程吞掉。两条反馈如果会产生不同响应、状态或副作用，就应保留为不同场景。

同时要防止另一种错误：最终反馈只能证明平台关注该业务结果，不能证明隐藏输入和断言是什么。Agent 可以用反馈扩充场景范围，但不能为了让场景显得完整而编造阈值、排序或边界。

最终冻结的第一份 Prompt 是：

```text
【请完成项目业务理解并创建 spec.md，本轮不实现业务代码】

【执行】

1. 阅读需求源文件@xx、项目测试样例脚本@xx、现有业务代码@xx，以及最终测试反馈{x}。
2. 先逐条分析最终测试反馈，再结合需求整理当前已知的完整业务场景目标；每条反馈都必须被某个场景明确覆盖，同义场景可以合并，但不能被笼统的主流程概括掉。
3. 创建 spec.md，按业务场景写清触发条件、操作过程和最终可观察结果，包括响应、主对象状态、关联对象变化及失败时不得改变的内容。
4. 最终反馈只用于确定需要覆盖的业务目标；题面未明确且存在多种合理实现的规则，记录具体歧义，不要自行选择。本轮先不拆 Task 或编写实现方案。
```

## 五、为什么第二份 Prompt 中的“重新核对”被忽略

### 经历者

我的第二份 Prompt 是：

```text
执行任务划分。

1. 重新核对 README、当前业务代码和 AICODING_STATE.md。
2. 把 P0 分配到可执行可实现的任务中，让每个任务形成一条从真实输入到可观察结果的完整业务路径，为每个任务写清业务结果。
```

第一步本来是希望 Agent 重新审查文档是否与需求一致，但实际执行时基本被忽略。并且当时还没有认识到测试脚本的价值：所有 Task 实现完后才统一补测试。我后来认为，应当让 Task、测试样例和最终验收场景建立对应关系。

### 分析者

“重新核对”表达了意图，却没有告诉 Agent 核对什么、发现问题后做什么。面对主要任务“执行任务划分”，Agent 很容易把核对理解成一次普通阅读，然后直接拆任务。

Agent 还会默认已经生成的文档是可信中间产物。第二次阅读 README 时，它倾向于用 README 帮助理解 `spec.md`，而不是主动质疑 `spec.md`。

所以核对应变成两个具体动作：

```text
README / 最终反馈 → spec.md：检查是否遗漏
spec.md → README / 最终反馈：检查是否冲突、无依据或把歧义写成事实
```

发现问题后先修正 `spec.md`，再划分 Task。这样复核才会对后续结果产生影响。

### 测试脚本为什么必须进入 Task 划分

一个 Task 只有能够形成下面的可执行路径，才真正可验收：

```text
准备测试数据
→ 调用公开接口
→ 获得当前响应
→ 查询主对象状态
→ 查询关联对象状态
→ 验证不应该发生的变化
```

如果 Task 只有“实现拒收”“完成派单”“处理结算”，实现 Agent 仍需临场猜测从哪个接口进入、怎样准备状态以及最后应该断言什么。

第二份 Prompt 不必立即把全部测试写进脚本，但必须为每个 Task 设计一个可以直接落地的验收样例。实际执行 Task 时，再在同一会话中实现代码并加入对应测试。这样才能建立：

```text
最终反馈
→ spec.md 业务场景
→ 实现该场景的 Task
→ 原测试脚本中的验收样例
```

当平台仍报告某个场景失败时，可以沿链路回查相关 Task、代码和断言。这个映射不能保证直接找到具体代码行，但能把问题从整个项目缩小到少量业务链。

### Task 应怎样划分

Task 应按完整业务结果划分，而不是按接口数量、代码文件或文档章节划分。多个接口共同产生一个结果时，应属于同一 Task；使用同一接口却产生不同副作用的场景，可以拆成不同 Task。

每个 Task 最少需要说明：

- 对应哪些业务场景和最终反馈；
- 从真实输入到最终结果的完整路径；
- 当前实现缺口和相关代码入口；
- 一个可以加入原测试脚本的验收样例。

仍有 A/B 歧义的场景可以建立 Task，但必须继续标出待判定规则，不能在任务划分阶段替题目决定答案。

最终采用的第二份 Prompt 是：

```text
【执行任务划分】

【执行】

1. 读取 @README.md、@spec.md、@当前业务代码、@原测试脚本和最终测试反馈{x}。不要默认 spec.md 正确：先逐条核对 README 与最终反馈中的已知场景是否全部进入 spec.md，并检查其中是否存在遗漏、冲突或被擅自确定的歧义；发现问题先修正 spec.md。
2. 把 spec.md 中的业务场景划分为可执行、可验收的 Task。每个 Task 应形成一条从真实输入、公开接口调用到最终可观察结果的完整业务路径，并写明对应的 spec 场景与最终测试反馈、业务结果和当前代码缺口。
3. 为每个 Task 设计一个可直接加入原测试脚本的验收样例，写清数据准备、调用顺序，以及对响应、主对象状态、关联对象变化和不得变化内容的断言。仍有多种合理解释的规则继续标明歧义，不要在任务划分时自行决定。
```

## 六、为什么分会话实现会放大 Task 之间的断裂

### 经历者

之后我切换了多个会话，让它们分别实现 Task 1、Task 2、Task 3……原始 Prompt 只是要求读取文件、实现指定 Task，并更新文档状态和真实结果。

问题是，每个业务对应的 Task 位于不同章节，彼此几乎没有联系。测试需要我手动运行，所以实现 Agent 更新的“真实结果”也没有意义。

### 分析者

多会话会放大文档中的任何边界缺口。新会话没有前一个会话的隐含理解，只能把自己的 Task 章节当作完整合同。

但隐藏评测通常验证的是跨 Task 的业务链：

```text
创建包裹
→ 揽收
→ 分拣
→ 路由推进
→ 派单
→ 派送
→ 签收或拒收
→ COD 收款与结算
```

Task 之间至少存在三类联系：

1. **流程依赖**：后一个 Task 的输入状态由前一个 Task 产生；
2. **共享状态**：包裹、运单、网点容量、快递员在途件数、COD 和客户余额会被多个 Task 修改；
3. **共同规则**：失败不改变状态、终态幂等、逻辑时间和 reset 横跨多个 Task。

如果实现会话只阅读自己的章节，它可能完成单个接口，却没有维护其他 Task 依赖的共享数据。后续会话又可能用另一种方式处理同一字段。每个局部测试都可能通过，完整业务链却失败。

因此，实现 Prompt 的重点不应重新复述第二份 Prompt 已经定义的业务结果，而应提醒实现者检查：

- 上游 Task 实际产生什么状态和数据；
- 当前 Task 修改哪些共享字段；
- 下游 Task 依赖什么结果；
- 跨 Task 的原子性、幂等和逻辑时间是否一致。

测试也不能只直接构造理想中间状态。验证签收时直接创建一个“派送中”包裹，只能证明签收函数能运行，不能证明揽收、分拣、路由和派送真的能够产生签收所需的数据。

## 七、测试现象怎样改变了根因优先级

### 经历者

冒烟测试、基础测试和进阶测试几乎全部通过，只有两三个完整业务的复杂测试没通过。这些测试往往涉及多个 Task，特别是流程依赖、共享状态和共同规则。我认为大概率是这些部分没有处理好。

### 分析者

这个新证据应当改变诊断优先级。

如果接口、字段、基本状态和单点规则普遍有问题，冒烟和基础测试不会大面积通过。现在只有少数完整业务场景失败，更符合跨 Task 集成故障的特征：

- 上游输出状态与下游输入条件不一致；
- 共享计数、容量或余额漏更新、重复更新或释放时机错误；
- 包裹与运单等关联对象没有同步；
- 原子性、幂等、逻辑时间或 reset 只在部分 Task 中实现；
- 单 Task 测试通过直接造状态，绕过了真实前置业务链。

因此，当前最可能的主因已经从“业务规则本身有歧义”转向“多个 Task 在完整业务链上的衔接失败”。业务歧义仍可能存在，但应当在本地完整业务链已经通过、平台仍失败之后再调查。

这也是第三份 Prompt 大修后的核心，不再保留此前把实现、运行、文档状态更新混在一起的版本：

```text
【请完成 Task{x} 的快速实现】

读取 @README.md、@spec.md、@全部业务代码和 @原测试脚本，按照 spec.md 实现 Task{x}，并将该 Task 已规划的验收样例加入原测试脚本。

实现时重点检查它在完整业务链中的衔接：上游 Task 产生的状态和数据、当前 Task 修改的共享状态、下游 Task 依赖的结果，以及跨 Task 共用的失败不变、幂等和时间规则。验收样例应尽量通过真实调用链到达目标状态，不能只直接构造中间状态。

本轮只修改业务代码和测试脚本，不更新文档，也不记录未经手动运行的测试结果。
```

实现完成后由用户手动执行测试，把实际输出贴回会话。普通语法、导入或断言错误可以直接纠正，不需要专门 Prompt。

## 八、为什么结果更新必须独立出来

### 经历者

Agent 无法主动获得测试结果，只能在我手动运行并粘贴之后更新文件。因此，文档更新不应继续放在第三份实现 Prompt 中。

### 分析者

这是必要修正。实现 Agent 实际知道的只有：

- 修改了哪些代码；
- 添加了哪个测试；
- 测试是否已经交给用户运行；
- 用户后来粘贴了什么输出。

它不能仅凭“已经写完代码”推断本地测试通过，更不能推断最终平台验收通过。

正确顺序是：

```text
实现会话修改代码和测试
→ 用户手动运行
→ 把终端结果粘贴回会话
→ 有错误则完成简单纠正
→ 用户重新运行
→ 结果稳定后单独更新文档
```

`spec.md` 不应重新变成详细状态管理文档。每个 Task 只需记录实际修改位置、对应测试、本地结果以及是否取得最终评测证据。

对应的小 Prompt 是：

```text
【更新 Task{x} 的实现记录】

读取 @spec.md、@业务代码和 @原测试脚本，根据我手动提供的测试结果{z}，在 Task{x} 下记录实际修改位置、对应测试及此次本地运行结果。只记录代码、测试和结果能够直接证明的事实；本地测试通过不等于最终评测通过，没有提供最终平台反馈时不得写为最终通过。
```

## 九、为什么顽固场景不能继续使用一条宽泛纠错 Prompt

### 经历者

原做法是运行最终验收，把失败业务场景贴给 Agent，让它根据场景反推测试用例和业务代码。我希望用基于 Prompt 1–3 的新 Prompt 替换它，并且可以按不同情况独立使用；每一步都应该给出具体操作，而不是宽泛目标。

### 分析者

平台场景名称只能告诉我们“哪个业务结果没有通过”，不能直接暴露隐藏输入和断言。如果立即要求 Agent 同时反推测试并修改代码，它很容易把完整业务场景错误归因到最后一个接口。

例如“COD 签收并完成结算”失败，根因可能位于创建 COD、路由推进、快递员占用、包裹/运单同步、签收幂等、客户余额或 COD 关闭中的任意一处。应先还原完整 Task 链，再找到第一个发生偏差的边界。

顽固问题应按证据路由：

| 现象 | 下一步 |
|---|---|
| 刚收到新的最终失败场景 | 定位该场景经过的全部 Task |
| 没有本地完整业务测试 | 建立带 Task 边界断言的全链路测试 |
| 本地全链路出现业务断言错误 | 找到第一个错误状态，而不是最后一个报错接口 |
| 本地全链路通过、平台仍失败 | 每次只调查一个真正未写死的 A/B 规则 |
| 根因已经由手动结果确认 | 修复产生错误状态的责任位置，并补回归断言 |
| 用户完成本地或平台复测 | 只记录实际结果，不补写推测 |

### 为什么要找“第一个错误边界”

最终断言失败往往只是上游错误的连锁结果。全链路测试应在每个 Task 边界检查主对象、关联对象和共享数据。用户把具体断言结果返回后，Agent 从第一个偏差开始比较：

```text
上游 Task 应产生什么
vs.
下游 Task 实际读取到什么
```

再检查所有相关字段的读取和写入位置，判断是否属于状态未传递、共享数据漏更新或重复更新、顺序错误、失败后残留、幂等失效、逻辑时间错误或 reset 残留。

### 什么时候才调查 A/B 歧义

只有本地全链路已经按照已知要求完全通过，而最终平台仍报告同一场景时，才说明现有测试和代码可能共享同一个错误解释。

此时也不能一次列出大量猜测。每轮只选择一个会改变最终结果、但题面没有唯一说明的规则：

1. 写出两种有依据的解释 A/B；
2. 找到它们会产生不同结果的唯一条件；
3. 增加一组只改变这个条件的对照测试；
4. 暂不修改业务代码；
5. 根据用户手动运行和下一次平台反馈决定后续方向。

本地对照测试本身不能证明平台采用 A 还是 B，它只是让后续修改具有可解释性。

### 修复为什么必须落到责任位置

当第一个错误边界已经确定，修复应落到产生错误状态或共享数据的实际责任位置，而不是在最终失败接口中增加当前样例特判。

共享数据需要在正确阶段更新且只更新一次；失败路径不能留下部分修改；重复调用不能重复产生副作用。修复后还要保留原接口，并在全链路测试中补充能够覆盖本次根因和相邻场景的回归断言。

完整的 Prompt 4A–4F 及使用顺序见 [AICoding 场景驱动实战 Prompt](./09-AICoding场景驱动实战Prompt.md)。

## 十、最终形成的方法

这次复盘没有证明我们能够准确还原隐藏测试，也没有把一次经验包装成普遍规律。它最终确认的是一条更符合真实做题条件的工作链：

```text
Prompt 1：用 README 和首次最终反馈建立当前已知的完整业务目标
Prompt 2：复核 spec，并建立场景—Task—验收样例映射
Prompt 3：在完整业务链中分会话实现一个 Task，并落地对应测试
用户手动运行：把 Agent 无法取得的本地和平台结果带回会话
Prompt 4：复杂失败先定位跨 Task 的第一个错误边界，必要时才调查规则歧义
```

最重要的变化可以概括为：

1. 不再以 P0 主流程代替已知场景覆盖；
2. 不再让 `spec.md` 只保存接口输入输出；
3. 不再让不同 Task 成为互不相干的章节；
4. 不再等全部实现结束后才统一补测试；
5. 不再把本地代码和测试的一致当成最终正确；
6. 不再根据最终场景名称直接猜隐藏测试；
7. 复杂失败优先检查流程依赖、共享状态和共同规则；
8. 只有本地完整业务链通过后，才有限地调查业务歧义。

这套 Prompt 的价值不是增加流程，而是在每一个容易产生无证据猜测的地方，建立一个能够由 README、公开测试、当前代码或用户手动结果支持的下一步。
