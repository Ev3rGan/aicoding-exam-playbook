# AICoding 分阶段实战 Prompt（稳健版）

> 每次只复制当前阶段的一段 Prompt。
>
> 根据题目难度删减条目，不要在开局一次性发送全文。
>
> 适合弱模型、Agent 能力未知、需要人工运行测试或错误容易反复的场景；高能力 Agent 的压缩路线见《[高智能体快速实现 Prompt](./06-AICoding高智能体快速实现Prompt.md)》。

## 0. 条目标记

- `【执行】`：Agent 本轮需要实际完成的动作。
- `【规范】`：执行过程中始终遵守的边界。
- `【输出】`：本轮结束时必须交付的内容。
- `【停止】`：完成后停在哪里，等待谁继续。

## 1. 状态文档

建议创建 `AICODING_STATE.md`，用于跨会话保存：

1. 已确认的 P0 需求与验收；
2. 业务代码、输入样本、正确示例、测试和配置等文件角色；
3. 业务任务（即纵向切片）及当前状态；
4. 已运行的测试与真实结果；
5. 当前错误、无效修改和下一步。

使用规则：

- README 和当前代码始终是一手事实；状态文档只是索引。
- 状态文档与 README 不一致时，先校准文档，再继续实现。
- 同一条状态直接更新，不反复追加长篇记录。
- 如果新增文件会影响判分，把状态文档保存在提交范围外，并在新会话中粘贴其内容。

## 2. 会话路线

1. **规划会话**：使用 Prompt 1～2，完成读题、建档、锁定 P0 和任务切分。
2. **实现会话**：使用 Prompt 3～5，一次完成一个切片的测试、实现和首次修复。
3. **止损会话**：同一错误连续两轮不变时关闭旧会话，使用 Prompt 6。
4. **平台反馈转化**：平台只给业务描述时，按需使用 Prompt 7 建立本地复现。
5. **审查会话**：实现结束后新开会话，使用 Prompt 8。

题目规模、流程强度和期望任务数量由候选人先决定；Agent 负责按这个决定切分，不替候选人扩大任务数量。

新建会话的原则：

- 简单题可以只用一个会话。
- 中等或复杂题在规划完成后新开实现会话。
- 测试、首次实现和第一次修复留在同一会话。
- 一个切片完成后，出现遗忘、臆想或长篇重复时再开新会话。
- 每个新会话重新 `@README.md`、`@AICODING_STATE.md`、当前代码和测试。

---

## Prompt 1：读题并建立状态文档

### 使用前

1. 新开规划会话。
2. `@README.md`。
3. `@main.py` 和其他入口代码。
4. `@` 能看到的测试入口、依赖或配置文件。
5. 如果平台验收可随时、免费运行，先运行一次并保存反馈。

### 直接复制

```text
请先完成“读题与现状确认”。

【执行】
1. 完整阅读我 @ 的 README、入口代码、测试入口和配置。
2. 确认你实际读取了哪些文件；缺少内容时列出需要我重新 @ 的文件。
3. 判断每个文件的角色：业务代码、待处理输入、异常样本、正确示例、测试入口或配置。
4. 创建或更新 AICODING_STATE.md。
5. 在状态文档中记录：
   - 文件角色；
   - 当前代码看起来已有但尚未验证的能力；
   - 必须完成的 P0 业务；
   - README 明示的输入、输出和关键约束；
   - README 给出的示例与验收；
   - 仍然不确定的内容。

【规范】
1. 只把我 @ 的文件和真实平台反馈当作事实。
2. README 描述的异常数据可能是待处理输入，不自动当作项目 Bug 修复。
3. 每项需求标记对应的 README 章节或示例，方便回查。
4. 不复述整篇 README。
5. 不修改业务代码或题目样本。
6. 不再创建第二份需求文档或 Spec。

【输出】
1. AICODING_STATE.md。
2. 文件角色摘要。
3. 仍缺少的必要文件。
4. 当前是否可以进入任务切分。

【停止】
完成上述内容后停止，不开始实现。

【修改前的平台反馈，可选】
在这里粘贴。
```

### 人工检查

1. 对照 README 检查状态文档。
2. 确认异常样本、输入数据和业务代码没有被混淆。
3. 只修正 P0 遗漏和明显理解错误。
4. 保留当前会话，继续 Prompt 2。

### README 与状态文档不一致时

```text
请暂停实现，校准 README 与 AICODING_STATE.md。

【执行】
1. 逐项找出状态文档与 README 的目标、文件角色和验收冲突。
2. 以 README 为需求源修正 AICODING_STATE.md。
3. 重新确认哪些是代码、待处理输入、异常样本、正确示例和测试。

【规范】
1. 本轮不修改业务代码或数据样本。
2. 不保留与 README 冲突的旧结论。
3. 不把状态文档当作高于 README 的需求源。

【输出】
1. 冲突清单。
2. 校准后的 AICODING_STATE.md。
3. 校准后是否可以继续原任务。

【停止】
完成校准后停止，等待我确认下一步。
```

---

## Prompt 2：锁定范围并切分任务

### 使用前

1. 重新 `@AICODING_STATE.md`。
2. 如果助手不会自动读取最新文件，重新 `@README.md` 和入口代码。
3. 由你先填写期望任务数量；`P0` 表示优先级，`任务` 与 `纵向切片` 在本文中是同一层交付单位。

### 直接复制

```text
请完成“范围锁定与任务切分”。

【执行】
1. 重新核对 README、当前代码和 AICODING_STATE.md。
2. 按我指定的数量切分任务；如果该数量明显无法容纳 P0，先指出原因，不自行改成其他数量。
3. 让每个任务本身就是一个“可观察的完整业务切片”。
4. 把 P0 需求分配到对应任务。
5. 为每个任务说明：什么输入经过哪个入口，应得到什么结果。
6. 把任务顺序和状态更新进 AICODING_STATE.md。
7. 按 P0 和依赖顺序给出建议的第一个任务，等待我确认。

【规范】
1. 不按文件、类、数据层或接口层横向切分。
2. 不在任务下面再制造一层含义不清的“最小 P0 切片”。
3. 不修改代码。
4. 不创建测试。
5. 不提前展开后续任务。

【输出】
1. 更新后的 AICODING_STATE.md。
2. 建议的第一个任务（即第一个纵向切片）。
3. 下一步需要 @ 的代码。
4. 准备使用的测试入口。

【停止】
完成任务切分后停止，不开始实现，等待我确认第一个任务。

【我指定的任务数量】
在这里填写，例如：1 个 / 3 个 / 不超过 4 个。
```

### 是否新开会话

- 简单题、当前上下文较短：继续当前会话。
- README 很长、存在多个切片或模型已经开始遗忘：新开实现会话。

---

## Prompt 3：为当前切片建立一个测试

### 使用前需要 `@`

1. `@README.md`。
2. `@AICODING_STATE.md`。
3. 当前切片涉及的代码。
4. 现有测试与测试配置。
5. 明确写出本轮唯一任务和本轮排除任务。

不要开局 `@` 整个项目；确实缺少依赖时再补充。

### 直接复制

```text
请只为“本轮唯一任务”建立一个测试信号。

【执行】
1. 重新读取我 @ 的 README、状态文档、代码和测试，并确认本轮唯一任务与排除项。
2. 从程序公开入口选择本轮唯一任务最关键的一个未验证行为。
3. 只编写一个能够识别该行为的最小测试。
4. 让预期结果来自 README、题目示例或明确推导。
5. 如果你能执行命令，运行测试并确认缺失行为能够失败。
6. 如果你不能执行命令，给出准确的运行命令和预期失败现象。

【规范】
1. 不为排除任务创建测试或实现。
2. 本轮不修改业务代码。
3. 不照着当前实现重新计算预期结果。
4. 不一次预写整批测试。
5. 主行为通过后，再用本 Prompt 补下一个边界或异常。

【输出】
1. 新增或修改的测试。
2. 需要运行的准确命令。
3. 该测试准备识别的业务缺口。

【停止】
不能执行测试时，写完测试和命令后立即停止，等待我返回真实结果。

【本轮唯一任务】
在这里填写。

【本轮明确不做】
在这里填写其他任务。
```

### 人工接力

1. 运行给出的命令。
2. 返回完整命令、输入、报错和堆栈。
3. 不要只回复“测试失败”。

如果测试意外通过：

- 先判断它是否确实覆盖目标行为；
- 覆盖正确：更新状态，寻找下一个缺口；
- 覆盖错误：重新使用 Prompt 3 修正测试；
- 不为了制造红灯而故意写错测试。

---

## Prompt 4：根据红灯做最小实现

### 使用前

1. 写明本轮唯一任务。
2. 写明本轮明确不做的其他任务。

### 直接复制

```text
请根据下面的真实测试结果完成当前切片的最小实现。

【执行】
1. 先在 AICODING_STATE.md 中确认本轮唯一任务和排除项。
2. 判断该失败是否准确命中当前缺失业务。
3. 如果测试本身有误，只修测试并让我重跑。
4. 如果失败符合预期，重新读取当前业务代码。
5. 只修改让当前测试通过所必需的代码。
6. 把当前任务更新为“已修改，待验证”。
7. 指出需要重新运行的同一个测试命令。

【规范】
1. 只实现“本轮唯一任务”，不实现排除项。
2. 不重写已经工作的代码。
3. 不进行无关重构。
4. 没看到复测结果前，不声称已经修复或通过。

【输出】
1. 本轮实际修改。
2. 更新后的 AICODING_STATE.md。
3. 需要复测的命令。

【停止】
修改完成后停止，等待我运行同一个测试。

【真实命令与输出】
在这里粘贴。

【本轮唯一任务】
在这里填写。

【本轮明确不做】
在这里填写其他任务。
```

### 人工接力

- 同一测试通过：运行相关回归测试，再使用“切片通过 Prompt”。
- 同一测试仍失败：进入 Prompt 5，不直接要求 Agent 再改一次。

### 切片通过 Prompt

```text
请记录当前切片的真实通过结果。

【执行】
1. 把通过的命令和对应行为写入 AICODING_STATE.md。
2. 把当前切片标记为“已本地验证”。
3. 指出下一个未完成的 P0 切片。

【规范】
1. 只记录我提供的真实结果。
2. 本轮不开始下一项实现。

【输出】
1. 更新后的 AICODING_STATE.md。
2. 下一个 P0 切片。

【停止】
更新状态后停止，不开始下一个切片。

【真实测试结果】
在这里粘贴。
```

---

## Prompt 5：测试仍失败时进行诊断

第一次失败不要换会话。先重新 `@` 刚修改的代码和测试，确保 Agent 看到磁盘中的当前版本。

### Prompt 5A：只诊断

```text
同一个测试在修改后仍然失败。请只诊断和缩小问题。

【执行】
1. 重新读取当前代码和测试。
2. 把真实输出与当前代码逐项对照。
3. 列出已经确定的事实。
4. 给出最多两个可能原因，并指出各自的代码或输出证据。
5. 选择一个最小检查，用来区分这两个原因。
6. 把本轮失败和上一轮无效修改简短写入 AICODING_STATE.md。

【规范】
1. 本轮不修改业务代码。
2. 不重复上一轮结论。
3. 不用“应该已经修复”代替测试。
4. 证据不足时，直接说明还要 @ 哪个文件或运行哪个测试。

【输出】
1. 已确认事实。
2. 最多两个候选原因。
3. 一个最小诊断检查及运行方式。

【停止】
给出诊断检查后停止，等待我返回结果。

【同一测试的最新完整输出】
在这里粘贴。
```

### Prompt 5B：根据新证据修一次

```text
请根据下面的诊断结果做一次最小修复。

【执行】
1. 根据新增证据选择一个原因。
2. 只实施一个最小修复。
3. 在 AICODING_STATE.md 中记录本轮假设和实际改动。
4. 把状态标记为“已修改，待验证”。
5. 指出需要重跑的原始失败测试。

【规范】
1. 不同时尝试多个方案。
2. 不重复没有新证据的旧修改。
3. 没看到复测结果前，不声称已经通过。

【输出】
1. 本轮修改。
2. 更新后的 AICODING_STATE.md。
3. 原始复测命令。

【停止】
完成一次修改后停止，等待复测。

【诊断检查结果】
在这里粘贴。
```

---

## Prompt 6：同一错误两轮不变时止损

### 使用前

1. 关闭旧实现会话。
2. 新开止损诊断会话。
3. `@README.md`。
4. `@AICODING_STATE.md`。
5. `@` 当前真实代码和测试。
6. 粘贴最近一次完整报错。

### 直接复制

```text
请执行一次止损诊断。前一个会话连续两轮修改后，同一错误仍然存在。

【执行】
1. 核对报错中的函数、行号与当前代码是否一致。
2. 阅读 AICODING_STATE.md 中最近两轮失败记录。
3. 指出两次修改为什么没有改变原始症状。
4. 把问题缩成当前仍能复现的最小输入或最小测试。
5. 给出运行该最小复现的方式。

【规范】
1. 不信任旧会话未经验证的根因结论。
2. 本轮不修改业务代码。
3. 不提出与前两轮相同的修复。
4. 只有出现新证据后，才允许进入下一次修改。
5. 没有新证据时，明确建议回退、跳过非 P0 能力或人工接管。

【输出】
1. 当前文件与报错是否一致。
2. 前两轮无效的原因。
3. 一个最小复现及运行命令。
4. 没有新证据时的止损建议。

【停止】
给出最小复现后停止，不做第三次盲改。

【最近一次完整报错】
在这里粘贴。
```

---

## Prompt 7：把平台反馈转换成本地测试

### 使用前需要 `@`

1. `@README.md`。
2. `@AICODING_STATE.md`。
3. `@` 相关业务代码和测试。
4. 粘贴平台反馈原文。

### 直接复制

```text
请把不可见的平台失败转换为一个本地可见测试。

【执行】
1. 把平台业务描述映射到 README 中最接近的需求。
2. 检查 AICODING_STATE.md 是否已经覆盖该需求。
3. 从公开入口设计一个本地可运行的最小示例。
4. 只编写一个用于复现该业务失败的测试。
5. 如果你不能运行，给出准确命令并等待我返回结果。

【规范】
1. 不猜隐藏测试源码或内部断言。
2. 未在本地复现前，不直接修改业务代码。
3. 一次只处理一个平台反馈场景。

【输出】
1. 对应的 README 需求。
2. 本地最小测试。
3. 测试命令和预期失败。

【停止】
建立测试后停止；确认本地复现后，再进入 Prompt 5。

【平台反馈】
在这里粘贴。
```

---

## Prompt 8：最终审查与收口

### 使用前

1. 停止增加新功能。
2. 新开审查会话。
3. `@README.md`。
4. `@AICODING_STATE.md`。
5. `@` 所有实际修改的业务代码。
6. `@` 自建测试并粘贴最近一次平台反馈。

### 直接复制

```text
请完成最终审查，本轮先不修改代码。

【执行】
1. 先检查 README 与 AICODING_STATE.md 的目标和文件角色是否一致。
2. 检查实际实现是否超出本轮批准的任务范围。
3. 第一遍按 README 审查需求：
   - P0 是否全部实现；
   - 基础示例、边界和异常是否有对应测试证据；
   - 是否存在需求遗漏、理解错误或只通过冒烟测试的情况。
4. 第二遍独立审查工程实现：
   - 入口、接口和数据格式是否一致；
   - 状态变化和异常处理是否正确；
   - 是否破坏已有能力；
   - 是否遗留会阻断验收的临时代码或明显风险。
5. 把文档校准、范围检查和两遍审查结果分开更新进 AICODING_STATE.md。
6. 按“必须修复、时间允许再修、明确未完成”排序。
7. 选出当前最应该处理的一个阻断项及其验证方式。

【规范】
1. 需求审查和工程审查分开，不互相抵消。
2. 冒烟测试通过不等于完整业务完成。
3. 本轮只审查，不一次重写全部代码。
4. 后续每个阻断项重新走“Prompt 3 → Prompt 4 → 复测”。

【输出】
1. 需求审查结果。
2. README/状态文档冲突与范围越界结果。
3. 工程审查结果。
4. 分级后的剩余问题。
5. 当前第一个阻断项和测试方式。

【停止】
输出审查结果后停止，等待我选择和执行修复。

【本轮批准任务】
在这里填写。

【本轮明确排除】
在这里填写。
```

### 最终结果 Prompt

```text
请记录最终测试和平台验收结果。

【执行】
1. 根据下面的真实结果更新 AICODING_STATE.md。
2. 分别列出：
   - 已实现且本地通过；
   - 已通过平台验收；
   - 已修改但尚未验证；
   - 仍未完成。

【规范】
1. 只使用我提供的真实测试结果。
2. 不用冒烟测试通过代替完整业务完成。
3. 不把“已修改”写成“已通过”。

【输出】
1. 更新后的 AICODING_STATE.md。
2. 诚实的最终完成状态与剩余风险。

【停止】
完成最终记录后结束本次流程。

【最终测试与平台结果】
在这里粘贴。
```

---

## 3 行记忆版

```text
规划：@README + @入口 → 建状态 → 锁 P0 → 切行为。
实现：@README + @状态 + @代码/测试 → 先测 → 最小实现 → 同一测试复验。
失败/收口：两轮不变就新会话缩小复现；最后先查需求，再查代码。
```

> **每个 Prompt 只推动一步；每个新会话重新交付事实；每次修改都回到同一个测试。**
