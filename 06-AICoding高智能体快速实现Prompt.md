# AICoding 高智能体快速实现 Prompt

> 基于《[分阶段实战 Prompt（稳健版）](./04-AICoding分阶段实战Prompt.md)》压缩。
>
> 适合能够自主读取项目、编写代码、运行测试并稳定遵守范围的高能力 Agent。
>
> 目标是在时间或 Token 受限时减少逐轮 red-green 对话，用批量实现、独立审查和定点修复快速收敛。

## 0. 与稳健版的区别

| 快速版 | 来自稳健版 | 调整 |
|---|---|---|
| Prompt 1 | 原 Prompt 1 | 删除逐文件读取确认，增加文件角色识别与目标对齐 |
| Prompt 2 | 原 Prompt 2 | 任务数量由人决定；任务就是纵向切片，不再嵌套“最小 P0 切片” |
| Prompt 3 | 替代原 Prompt 3～6 | 不执行逐测试 TDD；在限定范围内批量实现并自主测试 |
| Prompt 4 | 微调原 Prompt 8 | 新会话独立审查文档对齐、范围、需求和工程实现 |
| Prompt 5 | 新增 | 按“问题—解决方式—验证方式”逐项定点修复 |
| Prompt 6 | 新增 | 完整测试、平台验收与最终状态记录 |

选择快速版前，由候选人确认：

1. Agent 能较好理解长文档和多文件项目；
2. Agent 能自主运行并读取测试结果；
3. 候选人已大致判断任务规模和 Token 预算；
4. 批量实现失败后的返工风险可以接受。

任一条件明显不满足时，改用稳健版。

## 1. 会话路线

1. **规划会话**：Prompt 1 → Prompt 2。
2. **快速执行会话**：Prompt 3，一次实现候选人批准的一个或多个任务。
3. **独立审查会话**：Prompt 4，只找问题，不直接改代码。
4. **定点修复会话**：Prompt 5，一次处理一个审查或平台问题。
5. **最终收口**：Prompt 6。

每个新会话至少提供：

- `README.md`；
- `AICODING_STATE.md`；
- 当前相关代码、数据样本和测试；
- 上一阶段的真实测试或审查结果。

---

## Prompt 1：快速读题、识别文件角色并建档

### 使用前

1. 新开规划会话。
2. 指定 `README.md`。
3. 指定项目目录、数据样本、测试、依赖和配置。
4. 如果平台支持低成本验收，先保存一次基线结果。

### 直接复制

```text
请完成项目理解和状态建档，本轮不实现业务。

【执行】
1. 阅读 README，并检查当前项目、数据样本、测试和配置。
2. 先判断每类文件的角色：
   - 需要编写或修改的业务代码；
   - 待处理输入或异常数据样本；
   - 正确输出示例；
   - 测试与验收材料；
   - 配置和依赖。
3. 创建或更新 AICODING_STATE.md。
4. 记录 P0 业务、关键输入输出、验收示例、文件角色和当前待确认项。
5. 检查 README、项目现状和状态文档的目标是否对齐。

【规范】
1. README 是需求源，AICODING_STATE.md 只是可修正的执行索引。
2. 异常数据样本默认是待处理输入，不自动当作代码 Bug 修改。
3. 不重复生成需求分析文档或长篇 Spec。
4. 本轮不实现业务，不修复样本。

【输出】
1. AICODING_STATE.md。
2. 文件角色摘要。
3. 目标不一致或仍需人工决定的事项。
4. 用于候选人判断任务数量的复杂度依据。

【停止】
完成建档后停止，由我决定任务数量和执行路线。

【基线平台反馈，可选】
在这里粘贴。
```

---

## Prompt 2：按候选人指定数量建立任务

### 使用前

由候选人填写：

- `【任务数量】`：例如 1 个、3 个、不超过 4 个；

本文约定：

- `P0` 是需求优先级；
- `任务` 是交付单位；
- 每个任务本身就是一条可独立验证的纵向切片。

### 直接复制

```text
请按我指定的数量完成任务划分，本轮不写代码。

【执行】
1. 重新核对 README、项目现状和 AICODING_STATE.md。
2. 按我指定的任务数量，把 P0 和其他必要需求分配到任务中。
3. 让每个任务形成一条从真实输入到可观察结果的完整业务路径。
4. 为每个任务写清业务结果、验收方式和依赖顺序。
5. 把全部任务标记为“未开始”，不自行选择下一执行会话的范围。

【规范】
1. 不自行改变任务数量；数量明显不合理时先报告原因。
2. 不按文件、类或技术层拆任务。
3. 不在任务下面再嵌套一层“最小 P0 切片”。
4. 不修改代码和数据样本。

【输出】
1. 更新后的 AICODING_STATE.md。
2. 指定数量的任务及验收。
3. 每个任务预计使用的测试入口或命令。

【停止】
完成划分后停止，不提前实现，由我从已定义任务中选择下一会话的允许任务和排除任务。

【任务数量】
在这里填写。
```

Prompt 2 完成后，由候选人从已经定义的任务中选择 Prompt 3 的“本轮允许任务”和“本轮排除任务”。如果允许一次完成 Task 1～3，应明确写全，不能只写“实现 Task 1 对应切片”。

---

## Prompt 3：限定范围批量实现并测试

> 本 Prompt 替代稳健版 Prompt 3～6，不要求逐用例执行 red → minimal green。

### 使用前

1. 新开快速执行会话。
2. 提供 README、状态文档、当前项目和测试。
3. 再次填写允许任务与排除任务，不依赖上一个会话默认记住。

### 直接复制

```text
请完成本轮批准任务的快速实现和测试。

【执行】
1. 重新读取 README、AICODING_STATE.md、代码、数据样本和测试。
2. 先确认状态文档与 README 的目标、文件角色和验收仍然一致。
3. 只实现“本轮允许任务”；按依赖顺序完成其中的业务代码。
4. 为已实现行为补充必要测试，优先覆盖 README 示例、主流程和高风险边界。
5. 自主运行相关测试；失败时可以在批准任务范围内修复并重跑。
6. 相关测试通过后运行当前可用的完整测试。
7. 更新 AICODING_STATE.md，记录每个任务的实现状态、测试命令和真实结果。

【规范】
1. 不实现“本轮排除任务”。
2. 不把待处理数据样本当作项目源码修复，除非交付要求明确需要生成纠错后的输出。
3. 发现范围外问题时只记录，不顺手修改。
4. 不进行与本轮任务无关的重构。
5. 只有实际运行通过的测试才能标记为通过。
6. 遇到无法在当前范围解决的阻断项时停止，不擅自扩大范围。

【输出】
1. 实际完成的任务与修改文件。
2. 测试命令及真实结果。
3. 未通过测试、范围外发现和剩余风险。
4. 更新后的 AICODING_STATE.md。

【停止】
批准任务完成并运行测试后停止，不继续实现排除任务。

【本轮允许任务】
在这里填写。

【本轮排除任务】
在这里填写。
```

### 如果 Token 即将不足

按以下顺序保留工作：

1. 完成已经开始的 P0 路径；
2. 运行相关测试和完整测试；
3. 更新状态文档；
4. 停止新增能力，直接进入 Prompt 4；
5. 不用剩余 Token 做无关重构或长篇解释。

---

## 紧急 Prompt：README 与状态文档目标不一致

发现样本角色、业务目标或验收发生偏移时，立即暂停当前执行会话。

```text
请暂停代码实现，紧急校准 README 与 AICODING_STATE.md。

【执行】
1. 找出两者在业务目标、文件角色、任务边界和验收上的冲突。
2. 以 README 为准修正 AICODING_STATE.md。
3. 重新标记代码、待处理输入、异常样本、正确输出和测试。
4. 检查已经产生的修改是否建立在错误目标上。

【规范】
1. 本轮不继续实现或修复代码。
2. 不保留与 README 冲突的状态文档结论。
3. 对已经发生的错误实现只标记影响，不在校准阶段顺手重写。

【输出】
1. 冲突和受影响修改。
2. 校准后的 AICODING_STATE.md。
3. 可以继续、需要回退或需要重新实现的任务。

【停止】
校准完成后停止，由我决定如何处理受影响代码。
```

---

## Prompt 4：新会话独立审查

> 由稳健版 Prompt 8 微调而来。即使快速实现测试全绿，也执行本步骤。

### 使用前

1. 新开审查会话。
2. 提供 README、状态文档、全部实际修改、测试和真实结果。
3. 提供本轮批准任务与排除任务。

### 直接复制

```text
请独立审查快速实现结果，本轮不修改代码。

【执行】
1. 检查 README 与 AICODING_STATE.md 的目标和文件角色是否一致。
2. 检查实际修改是否超出本轮批准任务，或遗漏批准任务。
3. 按 README 审查需求覆盖、基础示例、边界、异常和完整业务链路。
4. 独立审查入口、接口、数据格式、状态变化、异常处理和回归风险。
5. 必要时运行只读检查或测试验证发现。
6. 把每个问题写成：
   - 问题描述；
   - 建议解决方式；
   - 验证方式。
7. 按阻断程度排序并更新 AICODING_STATE.md。

【规范】
1. 本轮只审查，不修改代码。
2. README 对齐、范围审查、需求审查和工程审查分别报告。
3. 冒烟测试通过不等于完整业务完成。
4. 不用笼统评价代替可定位的问题和验证方式。

【输出】
1. 文档与文件角色偏移。
2. 范围遗漏或越界。
3. 需求问题。
4. 工程问题。
5. 按优先级排列的定点修复清单。

【停止】
输出审查结果后停止，不自行修复。

【本轮批准任务】
在这里填写。

【本轮排除任务】
在这里填写。
```

---

## Prompt 5：单问题定点修复

每次只选择 Prompt 4、测试或平台反馈中的一个问题。

### 直接复制

```text
请只解决下面这一项问题。

【执行】
1. 重新读取与该问题直接相关的 README、代码和测试。
2. 确认问题在当前代码中仍然存在。
3. 按给出的解决方向实施最小修改；方向与当前代码冲突时先说明并采用更小的可验证方案。
4. 运行给出的验证方式。
5. 再运行与修改相邻的回归测试。
6. 更新 AICODING_STATE.md 中该问题和对应任务的状态。

【规范】
1. 一次只解决一个问题。
2. 不扩展到其他审查项或任务。
3. 不修改与问题无关的代码或数据样本。
4. 验证未通过时诚实保留失败状态，不声称完成。

【输出】
1. 实际修改。
2. 验证命令与真实结果。
3. 相关回归结果。
4. 该问题是否已经关闭。

【停止】
完成该问题的验证后停止，等待我选择下一项。

【问题描述】
在这里粘贴。

【建议解决方式】
在这里粘贴；没有时写“由 Agent 根据证据提出”。

【验证方式】
在这里粘贴。
```

### 整个任务缺失时

如果审查发现 Task 2、Task 3 等整个任务未完成，不把它们包装成一个“小问题”。回到 Prompt 3，把这些任务重新填入“本轮允许任务”，执行一次新的限定范围批量实现。

---

## Prompt 6：最终测试与交付状态

### 使用前

1. 所有阻断项已处理或明确放弃。
2. 提供 README、状态文档、当前代码和全部测试。
3. 如有平台验收，准备运行入口。

### 直接复制

```text
请完成最终测试和状态收口。

【执行】
1. 运行当前可用的完整测试。
2. 对照 README 和 AICODING_STATE.md 检查批准任务是否全部有真实测试证据。
3. 如果可以运行平台验收，执行并记录结果；不能执行时给出准确步骤，由我运行后返回。
4. 更新 AICODING_STATE.md。
5. 分别列出：
   - 已实现且本地通过；
   - 已通过平台验收；
   - 已修改但尚未验证；
   - 明确未完成。

【规范】
1. 不在最终收口阶段新增功能。
2. 不用冒烟测试代替完整业务验证。
3. 不把“已修改”写成“已通过”。
4. 只报告实际运行得到的结果。

【输出】
1. 完整测试与平台结果。
2. 更新后的 AICODING_STATE.md。
3. 最终完成状态和剩余风险。

【停止】
完成记录后结束本次实现流程。
```

---

## 3 行记忆版

```text
人定规模：读 README、辨文件角色、定任务数量和本轮范围。
Agent 快做：只批量实现批准任务，自主测试，完成即停。
新会话收口：独立审查；一个问题一次定点修复；最后完整验收。
```

> **强 Agent 可以少分轮，但不能少范围、测试、审查和验证。**
