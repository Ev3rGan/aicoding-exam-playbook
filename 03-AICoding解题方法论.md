# AICoding 笔试解题方法论

> 版本：v1.1 初版
>
> 目标：在通常只有 1～2 小时、题面较长、Coding Agent 能力和工具权限不确定的情况下，尽快交付尽可能完整、可验证的业务实现。
>
> 适用边界：本方法兼容能够自主执行测试的 Agent，也特别考虑了弱模型、手动 `@` 文件、人工运行测试和平台验收不透明的场景。

本文由《[Skills 机制拆解与映射](./01-Skills机制拆解与映射.md)》以及两次真实经历共同提炼：《[第一次经历](./02-真实AICoding笔试经历.md)》提供弱模型、人工测试接力和反复报错场景，《[第二次经历](./05-第二次真实AICoding笔试经历.md)》提供高能力 Agent、批量实现越界与独立审查收口场景。两次经历用于校准方法，不被当作全部平台的固定规律。

## 一、核心判断

AICoding 笔试不是比谁生成代码更快，而是比谁能让有限能力的 Agent 持续接触真实上下文，并在时间耗尽前把需求收敛成可验证实现。

整个过程记成七个字：

> **读 — 锁 — 切 — 测 — 做 — 修 — 收**

```text
可选基线测试
     ↓
读题 → 锁范围 → 切主链 → 测出缺口 → 做最小实现 → 复测
                              ↑                       │
                              └──────── 修复 ←────────┘ 仍失败
                                                      │通过
                                                      ▼
                                                  下一切片 → 收口
```

这套方法保留了完整工程 Skills 中最重要的推理和验证纪律，但删除了多数笔试不需要的 Issue、ADR、多 Agent、工作树、PR 和发布治理。

## 二、七步主循环

### 1. 读：先接触事实，不靠模型猜题

第一轮至少让 Agent 看到：

- 完整 `README.md`；
- 当前入口代码和已提供的基础实现；
- 能看到的测试入口、示例和运行方式；
- 已经出现的原始报错或平台反馈。

读取后先确认每类文件在题目中的角色：业务代码、待处理输入、异常样本、正确示例、测试入口或配置。尤其在数据评估与纠错题中，“包含错误的数据”可能正是输入，不是应该被修掉的项目文件。

弱模型没有看到文件时，应由它明确请求重新 `@`，而不是根据文件名想象内容。README、当前代码和真实测试输出始终是一手事实；任何需求摘要或 `spec.md` 都只是辅助索引。

如果平台允许随时、低成本地触发验收，可以在修改前先跑一次，记录初始通过情况和暴露出的业务场景。它是基线，不是让模型猜隐藏测试实现的材料。

### 2. 锁：锁住需求覆盖，不重复抄写 README

默认不连续创建“需求分析文档 + Spec + 实现计划”三份相似材料。长 README 更适合被压成一份短工作清单，说明：

- 必须完成的 P0 业务；
- 输入、输出、状态和关键约束；
- README 已给出的正常、边界与异常示例；
- 每项目前是未实现、待验证还是已验证。

简单题可以只在对话中列清单；中等题保留一份短工作记录；只有需求关系复杂、需要跨会话时，才值得创建独立 Spec。即使创建了 Spec，实现时仍应同时提供 README，防止摘要遗漏被继续放大。

锁范围的完成标志不是“文档写完”，而是知道先交付什么、怎样观察它成立、哪些暂时不做。

如果工作清单、Spec 或状态文档与 README 不一致，应暂停实现并立即校准：以 README 为准找出冲突，修正文档中的目标和文件角色，再继续编码。状态文档是可更新的索引，不能因为进入了新会话就升级成新的需求源。

### 3. 切：按可观察行为切，不按文件或技术层切

优先打通最小的端到端业务路径。例如，不先分别完成“全部数据结构、全部算法、全部接口”，而是选择一个真实输入，让它经过必要逻辑得到正确输出，并为这条路径补上测试。

切片顺序通常是：

1. P0 主链和最小有效输入；
2. README 明示的基础示例；
3. 边界、异常、重复调用或状态变化；
4. 进阶能力与质量优化。

小题可能只有一个切片；中等题通常拆成 2～4 个；复杂题才需要更多阶段。到考试时间中点时，最好已经有一条真实 P0 链路通过，而不是只有若干尚未组合的模块。

题目规模、流程强度和期望任务数量最好由候选人先做判断，Agent 负责把这个决定映射成可执行行为。为避免术语混乱，本方法约定：

- `P0` 是必须优先完成的需求等级，一项或多项 P0 可以分布在多个任务中；
- `任务` 是实际推进单位，每个任务应当对应一条可独立验证的纵向切片；
- 如果一个任务仍然过大，就把它拆成两个更小任务，而不是再制造一层含义模糊的“任务内最小 P0 切片”。

### 4. 测：先建立能识别当前行为的测试信号

测试应尽量从题目实际入口或公开函数观察行为，预期结果来自 README、明确示例或人工推导，不能照着实现代码重新计算一遍。

每个切片优先覆盖：

- 一个正常行为；
- 当前最危险的边界或异常；
- 如果业务有状态，再覆盖重复调用或状态变化。

对于尚未实现的行为，先确认测试确实能够失败，再进入实现；对于已经存在的能力，可以先用通过的测试固定当前行为，再为缺失部分增加失败用例。

能自主执行命令的 Agent 应自己运行并读取结果；不能执行命令的助手必须先给出测试代码和准确运行方式，由人执行后原样回传输出。这样测试不是实现完成后的补充说明，而是下一步改动的目标信号。

平台验收适合放在三个节点：开局基线、P0 主链完成后、最终收口前。如果平台次数受限或运行成本高，则减少为 P0 和最终两次。

### 5. 做：只写让当前测试通过的最小实现

每轮修改前，让 Agent 重新读取当前相关代码，而不是依赖上一轮记忆。只完成当前行为所需的最小改动，保留已经工作的路径，暂缓无关重构、通用框架和“以后也许有用”的扩展。

修改后立即重跑同一个测试。在看到结果前，代码只能标记为“已修改，待验证”，不能说“已经通过”。第一版代码生成得快并不代表进度快；真正的进度是当前业务行为已经被真实测试观察到。

### 6. 修：围绕同一个失败收敛，不让模型反复表演修复

出现错误后，先固定四样东西：当前真实代码、原始失败输出、触发失败的同一个测试、已经尝试过但无效的修改。

随后让 Agent 先解释最可能的原因和证据，再做一个最小修复。修改后必须重跑原来的失败用例；只有它从失败变为通过，才说明本轮命中了问题。新错误出现时，应先判断它是原问题推进后的下一层，还是修改造成的回归。

如果同一症状连续两轮仍然存在，应立即止损：

- 停止第三次近似修改；
- 重新 `@` 当前文件和完整输出；
- 缩小为更小的可复现输入；
- 对比前两轮究竟改了什么；
- 必要时回到最后一个可工作的版本，换一个有新证据的方案。

新会话和 handoff 只能搬运信息，不能自动产生诊断能力。如果没有新证据，新会话很可能重复旧错误。

### 7. 收：先查需求，再查代码，最后跑完整链路

最终收口分两遍：

1. **需求检查**：逐项回看 README，确认 P0、基础示例、边界和异常是否有对应实现与测试；找遗漏、误解和范围外发挥。
2. **工程检查**：检查入口是否跑通，接口和数据格式是否一致，是否破坏原有能力，是否遗留临时代码、重复逻辑或明显异常处理缺口。

修复阻断项后，重跑本地相关测试和完整测试，再触发最终平台验收。最后明确区分：已经实现、已经本地验证、已经平台验证、尚未完成。冒烟测试通过只能证明链路存在，不能代替完整业务验收。

## 三、两条必须保护的反馈循环

### 实现循环

```text
一个需求 → 一个纵向切片 → 一个可失败测试 → 最小实现 → 同一测试通过
```

它防止 Agent 一次生成全部业务后才发现总体方向错误。

### 诊断循环

```text
同一失败 → 基于当前代码提出原因 → 最小修改 → 重跑同一失败
```

它防止 Agent 用长篇分析、重复改动和自信声明代替真实修复。

## 四、按题目和工具能力自适应

### 先由人选择执行路线

Agent 可以提供建议，但最终路线由候选人根据题目、Agent 能力、Token、时间和测试权限决定。

**稳健路线**适合弱模型、能力未知、需求风险高或错误容易反复的场景：

```text
一个任务 → 一个测试 → 最小实现 → 复测 → 下一个行为
```

**快速路线**适合高能力 Agent、时间或 Token 紧张，并且能够及时运行测试的场景：

```text
人锁定本轮任务和排除项
→ Agent 批量实现选定任务并运行测试
→ 新会话独立审查
→ 按“问题—解决方式—验证方式”逐项修复
```

快速路线可以省略逐测试的 red-green 对话，但不能省略范围、测试、审查和最终验证。Agent 越强，越要明确本轮允许实现哪些任务、哪些任务不做、运行什么测试后停止，防止快速越界。

对应的可复制文本分别见《[分阶段实战 Prompt（稳健版）](./04-AICoding分阶段实战Prompt.md)》和《[高智能体快速实现 Prompt](./06-AICoding高智能体快速实现Prompt.md)》。

### 简单题

不创建额外文档。读完题后列出 P0、入口和验收，直接完成一个切片并测试。

### 中等题（默认）

保留一份短工作清单，拆 2～4 个纵向切片。每完成一个切片就验证，P0 完成后跑一次平台验收。

### 复杂题

建立简短 Spec 或需求覆盖表，明确切片依赖和阶段验收。仍然从一条 P0 主链开始，不先铺开全部模块。

### Agent 不能执行测试

把人当作测试运行器：Agent 负责生成测试和命令，人负责执行并原样返回结果。每轮只推进一个行为，减少人工搬运和上下文混乱。

### 平台测试不可见

把平台反馈当作业务证据，不猜测试源码。根据反馈描述回到 README，构造一个本地可见的最小业务用例；先让它失败，再修复并回归。

### 审查后需要快速修复

最终审查或平台反馈已经把问题定位到单项行为时，不必重新走完整规划流程。一次只处理一个问题，并同时给 Agent：

1. 具体问题及当前表现；
2. 期望采用的解决方向；
3. 判断问题已经消失的验证方式。

这种定点修复适合快速路线的收口，也适合 Token 不足时优先清除阻断项。

## 五、时间控制

时间分配不必机械计时，但应守住三个检查点：

- 前段尽快完成读题、基线和 P0 清单，不沉迷写文档；
- 时间过半前打通至少一条 P0 真实链路；
- 最后约四分之一停止扩展新功能，专注需求覆盖、回归和平台验收。

同一错误连续两轮或持续约十分钟没有新证据，就执行止损。宁可保留一个稳定的较小实现，也不要在最后阶段让大范围修改破坏已经通过的能力。

## 六、记忆版

> **事实不猜，范围锁全；行为切小，改动做窄；测试要真，修复复验；两轮止损，双轴收口。**
