# 一次真实 AICoding 笔试经历

> 状态：访谈记录 v0.1
>
> 用途：保存一次真实 AICoding 笔试中的环境、操作、结果与主观感受，为后续解题 Prompt 的设计提供场景证据。
>
> 证据边界：本文首先忠实记录当事人的回忆与判断；在获得更多场次或平台证据前，不把单次经历直接表述为所有 AICoding 笔试的普遍规律。

## 1. 笔试题目与时间约束

- 笔试直接提供一份内容很长的 `README.md`，以及已经创建好的基础代码 `main.py`。
- 总作答时间约为 1～2 小时。
- 在这个时间窗口内，候选人很难靠自己完整理解需求并掌握全部关键业务逻辑，实际完成高度依赖 Coding Agent。
- 即使实现过程中出现报错，候选人也很难在短时间内独立接管复杂调试，通常仍需依赖 Coding Agent 自行纠错。
- 平台提供一个 `test_xx.py`，但候选人大概率不能直接运行或查看其中的测试，只能通过笔试提供者的入口触发验收。
- 平台验收可以在 build 的任意时间点触发；结果只显示通过情况和大致的业务用例描述，看不到测试源码和完整执行过程。

## 2. Coding Agent 的实际形态与能力边界

这次使用的工具与 Codex 一类能够读取工作区、执行命令、观察输出并持续迭代的自治 Coding Agent 有明显差异。它更接近传统 AI 编程助手：

- 在本地浏览器中提供一个对话框和 IDE；
- 使用的模型智能程度相对有限；
- 必须由用户手动 `@` 指定文件，模型才能较可靠地读取内容；
- 未显式引用文件时，模型可能臆想代码或文档内容；
- 能够读取、编写和编辑文件；
- 无法在对话框自己的 sandbox 中真正执行命令；
- 候选人可以在 IDE 中手动运行自行设计的测试，并看到这些测试的报错和堆栈；
- 平台的 `test_xx.py` 大概率不能直接运行，完整验收由后台入口执行，用户只能看到通过情况和大概的业务场景描述。

这意味着候选人可以建立**人工中转的可见测试循环**，但无法直接复用完整的“Agent 读取代码 → 执行测试 → 观察报错 → 修改 → 重跑”自治反馈循环。文件是否被模型看到、测试如何触发、错误信息如何重新送回模型，都需要候选人显式管理；平台验收则是一个可随时触发、但内部不透明的第二反馈通道。

## 3. 本次实际操作路径

本次作答大体复用了参考文章《[AICoding 笔试方法论：从需求拆解到提交复盘](https://github.com/Ceceliawai/Agent/blob/main/docs/notes/aicoding/index.md)》给出的方向：

1. 手动让编程助手读取 `README.md`；
2. 让它先创建一份需求分析文档；
3. 再根据需求分析生成 `spec.md`；
4. 基于 `spec.md` 进入代码实现；
5. 使用自行创建的测试代码检查实现；
6. 将出现的报错继续反馈给编程助手，尝试反复修复；
7. 通过平台提供的测试入口进行最终验收。

这一流程提供了大致方向，但不像本地 Skills 那样，对 test seam、纵向切片、red-green、诊断反馈回路、双轴 Review 和停止条件作出精细约束。

### 3.1 事后提出、尚未验证的流程假设

由于平台验收可以在 build 的任意阶段触发，一个可能的改进是：**在创建需求文档或技术文档前先运行一次平台验收**，利用反馈中的业务场景描述反向识别评分重点和验收边界。

另一个尚未确认的判断是：题目已经提供信息完整的长篇 `README.md`，单独生成“需求文档”可能只是重复压缩信息，甚至引入遗漏；更有价值的中间产物也许应该直接服务于需求覆盖、实现顺序和测试验收。两点都需要在后续方法论阶段评估，不能仅凭一次经历确定。

## 4. 实际结果与主要问题

### 4.1 需求分析与 `spec.md` 没有形成足够强的实现契约

生成需求分析和 `spec.md` 后，最终实现能够通过一些冒烟测试、少量基础用例和复杂用例，但距离完整业务实现仍有明显差距。回看这份较简陋的 `spec.md`，主要问题同时包含 README 需求遗漏和业务理解错误。

虽然 `spec.md` 中存在功能清单以及测试、验收映射，但实现阶段的模型经常忽略其中的测试和验收要求：需要后续追加 Prompt，它才会补写测试代码；平台验收又只能由候选人亲自触发并把结果转述给模型。候选人甚至需要同时 `@README.md` 和 `spec.md`，以降低只参考二手 Spec 后继续臆想业务的风险。

这说明“先生成一份 Spec”本身并不能保证实现完整。至少在本次经历中，同时存在两类问题：一是 Spec 自身可能遗漏或误解需求；二是即使 Spec 写出了测试和验收，弱模型在实现时也不会稳定遵守。需要进一步约束：

- 每项需求是否都被实现覆盖；
- 需求之间的组合行为；
- 边界条件和异常路径；
- 从哪个可观察入口验证完整业务；
- 实现失败后如何定位缺失能力，而不是只修当前样例。

### 4.2 最大瓶颈出现在实现与测试纠错阶段

当时的主观感受是：该编程助手的能力大致接近 2025 年年初 Coding Agent 的水平。它能够较快生成第一版代码，但自行创建测试后会出现报错；即使不断优化 Prompt，也无法稳定地独立定位和修复问题，类似错误会反复出现。

候选人已经尝试在反馈中区分真实内容、待验证内容、已验证内容和已经修改的内容，并提供报错信息辅助调试。模型会进行大量逻辑分析，随后非常肯定地宣布修改完成；但实际重新运行测试时，原错误仍然存在。典型表现包括：

- 重复生成相同错误；
- 臆想部分代码或当前文件状态；
- 做了大量分析，却再次实施与之前近似的无效修复；
- 用“已经修复”的语言替代对原始失败是否消失的实际验证。

为打破这个循环，候选人还尝试过：

- `@` 所有相关代码，让模型重新基于完整代码诊断；
- 创建 handoff 文档，单独记录报错、真实/待验证状态和已经尝试过的修改；
- 把 handoff 交给另一个新会话重新执行调试。

这些方法仍未使报错稳定消失。就本次经历而言，没有发现一种能够稳定打破重复失败循环的操作。单纯增加上下文、整理交接信息或更换会话，并不足以让弱模型获得可靠的诊断和修复能力。

本次大约 **80% 的时间**消耗在“运行自建测试 → 发现业务错误 → 修改 → 原错误仍在 → 再次修改”的循环中。因此，最影响结果的不是第一版代码生成、需求文档编写或 Git 管理，而是：

- 如何让弱模型一次只完成可控的实现增量；
- 如何在模型不能自主执行命令时建立有效反馈；
- 如何把测试失败完整、准确地重新输入给模型；
- 如何阻止模型在多轮修复中重复犯错或扩大改动；
- 如何判断当前修改真正解决了原始问题，而不是只让一个局部样例通过。

### 4.3 最终进展没有到达完整业务验收阶段

由于大量时间消耗在中间 phase 的实验验证和反复修复上，`spec.md` 中的必要内容没有全部实现。阶段性结果为：

- 冒烟测试全部通过；
- 基础示例只有少量通过；
- 进阶示例只有少量通过；
- 完整业务实例未通过。

具体失败实例受平台记录能力限制，事后无法完整还原。但这不是本案例最关键的问题：当前进展甚至没有收敛到“必要功能全部实现，再进行最终平台验收”的阶段。因此不能把结果简单描述成“完整实现后被隐藏测试击败”，更准确的说法是：**实现—测试循环在中间阶段失去收敛，导致范围内的必要业务没有完成。**

## 5. 对后续解题 Prompt 的直接启示

以下只是由本次经历产生的设计约束，还不是最终方法论：

1. **实现与测试应成为 Prompt 的重心。**需求分析需要足够，但不应占用与实现纠错同等篇幅。
2. **不能假设 Agent 可以自主运行命令。**Prompt 需要定义“模型输出操作 → 人工运行/后台验收 → 原样回传结果 → 模型诊断”的协作协议。
3. **必须显式管理文件上下文。**每轮应提醒用户 `@` 当前权威需求、相关代码、测试入口和最新错误，避免模型凭记忆或想象工作。
4. **Spec 必须能建立需求覆盖，而不只是复述 README。**需要某种轻量的需求—实现—测试对应关系，防止只完成冒烟链路。
5. **修复循环需要比普通‘请继续修复’更严格。**应保留精确症状、最小修改、禁止无关重写、失败用例与回归验证等约束。
6. **Git 可以降级。**除非平台明确要求版本管理，否则 Git 不应占据通用解题 Prompt 的主要篇幅。
7. **需要防止模型用完成声明代替复验。**每轮修复必须回到同一个原始失败，明确报告它是否消失；没有复验结果时只能说“已修改，待验证”。
8. **需要抑制无效重复。**下一轮修复前应记录上一轮假设、修改和复验结果；原错误仍在时，不应在没有新证据的情况下重复同一修法。
9. **handoff 不是诊断能力的替代品。**完整记录报错和历史修改仍然没有解决问题，未来 Prompt 必须约束模型如何使用这些证据，而不只是要求“阅读 handoff 后继续修复”。
10. **应重点提炼 `implement`、`tdd`、`code-review` 的执行纪律。**同时必须针对弱模型降低指令复杂度，并改写成适合人工逐段输入、口语化表达和手动运行测试的形式。

## 6. 本轮访谈结论

已经确认：

1. 自建测试可手动运行并查看堆栈，平台验收可随时触发但内部不透明；
2. 修复循环的核心故障是模型丢失当前代码事实、重复无效修复，并把修改声明误当成验证结果；
3. `spec.md` 同时存在需求遗漏/误解，以及实现阶段不遵守测试验收的问题；
4. 第一版实现较快，大约 80% 时间消耗在无法收敛的测试—修复循环。
5. 冒烟测试通过，但基础和进阶示例仅少量通过，完整业务实例未通过；工作未推进到必要功能完成后的最终验收阶段。
6. `@` 全部代码、创建详细 handoff、换新会话均未形成稳定有效的破局方法。

本轮关键访谈到此关闭。后续不再追问平台或题目细节；这份记录将作为设计弱模型、手动输入型解题 Prompt 的场景约束。
