# 第二次真实 AICoding 笔试经历：高能力 Agent 场景

> 状态：经历记录 v0.1
>
> 用途：补充与第一次弱模型经历不同的场景，记录高能力 Agent 在文档理解、范围控制、Token 取舍和紧急收口中的表现。
>
> 证据边界：模型可能为 Sonnet 4.6 是根据智能程度作出的个人推测，并非平台确认信息。

## 1. 场景与 Agent 能力

- IDE 类似 VS Code，提供插件和 Git 管理，但本次作答没有使用 Git 流程。
- Agent 使用 Claude Code，能够理解较清晰的意图、指令和规范，整体可以承担大部分开发任务。
- 项目没有提供原始入口代码，需要从零实现。
- 项目提供了存在问题的数据示例，目标是对数据进行评估和纠错。

这次经历与第一场的核心差异是：主要风险不再是模型完全无法修复错误，而是**目标识别、范围边界和流程取舍没有说明清楚时，高能力 Agent 会快速执行一个并非用户真正想要的更大任务。**

## 2. 规划阶段出现的三个问题

### 2.1 示例数据被误判成待修复代码

实际使用了《AICoding 分阶段实战 Prompt》中的 Prompt 1 和 Prompt 2，但只输入了大致的 `【执行】`、`【规范】` 和 `【输出】`，没有输入 `【停止】`。考虑到 Agent 能力较强，也删除了“确认是否真正读取文件”的步骤。

Agent 在读取项目时，把题目刻意提供的异常数据示例识别成了代码缺陷，并报告准备尝试修复。由于输出要求没有明确要求说明“每个文件在题目中的角色”，这个目标偏移没有第一时间暴露。后续由用户主动指出，并修正了 `AICODING_STATE.md` 与 README 之间可能存在的目标不对齐。

这说明仅仅确认“读取了哪些文件”还不够；对于数据处理、纠错和评估类题目，更重要的是先区分：

- 哪些是需要实现的代码；
- 哪些是待处理输入或故障样本；
- 哪些是正确输出示例；
- 哪些是验收或测试材料。

### 2.2 任务数量不适合完全交给 Agent 判断

Prompt 1 从 README 中提取了 10 项 P0，Prompt 2 又把实现拆成 3 个任务，每个任务对应一个切片。用户无法从输出中清楚判断“P0、任务、切片、最小 P0 切片”之间的层级关系。

“一个任务对应一个纵向切片”本身未必错误；真正的问题是当前术语容易让人以为任务下面还必须继续拆一个“最小 P0 切片”。本次经历表明，后续应考虑：

- 题目规模和期望任务数量由候选人先做简单判断；
- Agent 负责把已确定的数量映射成可执行行为；
- `P0` 表示优先级，`任务/切片` 表示交付单位，不应混成三级嵌套概念；
- 如果一个任务已经是一条可独立验证的端到端行为，就不必为了术语再拆一层。

### 2.3 技术文档与 README 发生偏移时需要立即校准

`AICODING_STATE.md` 一旦误解了项目目标，后续新会话会把错误理解当成新的事实源继续执行。用户本次通过重新指出 README 的真实目标并手动修正状态文档，避免了偏移继续放大。

对后续 Prompt 有意义的候选处理方式是：

1. 发现 README 与状态文档不一致时，暂停实现；
2. 明确 README 是需求源，状态文档只是可修正的索引；
3. 找出冲突条目并用 README 原意覆盖；
4. 重新确认输入样本、问题对象和交付代码的角色；
5. 校准完成后再进入实现，不同时继续写代码。

## 3. 实现阶段的范围越界

由于对 Agent 能力和 Token 限额作出了不准确估计，本次没有继续使用 Prompt 3～6，而是新开执行会话，直接要求 Agent 按 `AICODING_STATE.md` 实现 Task 1 对应切片。

实际结果是：Agent 一次实现了 Task 1、Task 2 和 Task 3 的全部内容。当前切片虽然定义了 Task 1 的行为边界、验收方式和标准，但执行指令没有把“只允许完成 Task 1”“Task 2/3 本轮不做”“达到哪个状态后停止”说清楚。

这次越界没有直接导致最终失败，但暴露出一个与弱模型不同的问题：

- 弱模型需要防止做不完和反复失败；
- 高能力 Agent 还需要防止做得太快、太多，并把未审核范围一起实现。

`【停止】` 信息的缺失可能增加了这一风险，但单次经历不足以证明它是唯一原因。更直接的缺口是：实现会话没有再次锁定本轮唯一任务和明确排除项。

## 4. Token 受限时采用的快速路线

本次没有严格执行逐条测试的 red → minimal green 循环，而是采用了更短的路线：

```text
状态文档与任务划分
→ 高能力 Agent 批量实现
→ 新审查会话检查完整性
→ 针对发现的问题补齐实现并重跑测试
→ 对最后单项问题定点修复
```

Prompt 8 的最终审查确实发现了一些问题。之后用户在执行会话中使用自定义 Prompt：

1. 补齐 Task 2、Task 3 对应的切片内容；
2. 重新运行测试；
3. 修复审查发现的两个问题；
4. 对剩余的 LLM 调用分析字段问题，单独提供问题描述、解决方式和验证方式；
5. 要求 Agent 只解决这一项并验证。

最终提交时，大部分业务实例通过。

这说明在 Agent 能力较强、Token 或时间紧张时，严格逐测试 TDD 可能不是唯一可行路径。可以进一步研究一种快速路线：

- 人先锁定任务规模与执行边界；
- Agent 按一个或多个完整任务快速实现；
- 立即运行测试并启动独立审查；
- 审查问题逐项定点修复；
- 每个问题仍然保留“问题描述 → 解决方式 → 验证方式”。

它可以减少逐轮对话成本，但不能删除测试、审查和最终验证。其适用前提是 Agent 足以理解整体任务，并且批量实现失败后的返工风险可以接受。

## 5. 对后续 Prompt 有意义的候选启示

### 5.1 先由人选择流程强度

候选人应先评估：

- Agent 能力强弱；
- 题目规模；
- Token 和剩余时间；
- 是否有现成代码；
- 测试能否自主执行。

然后选择“逐切片 TDD”或“批量实现 → 测试 → 审查 → 定点修复”。不宜让 Agent 同时决定题目规模、任务数量和流程强度。

### 5.2 实现会话需要重新声明本轮边界

即使状态文档已有完整任务图，新实现会话仍应明确：

- 本轮只实现哪个任务；
- 哪些任务明确不做；
- 运行哪些测试后停止；
- 是否允许顺手处理其他发现。

高能力 Agent 不一定会自动把“当前 Task”理解成“唯一允许修改的范围”。

### 5.3 审查可以作为跳过完整 TDD 后的紧急恢复点

本次直接进入最终审查后，成功发现了批量实现留下的问题。说明当 Token 或时间不足、无法完整执行 Prompt 3～6 时，独立审查仍是重要的恢复手段。

但审查只能发现问题，不能替代验证。发现的问题仍需要逐项回到测试与修复。

### 5.4 定点修复比笼统“解决剩余问题”更有效

本次最后采用的有效表达包含三部分：

1. 当前具体问题；
2. 期望解决方式；
3. 验证该问题是否消失的方法。

Agent 每次只处理一个问题，修复后立即验证。这种表达适合：

- 最终审查发现的阻断项；
- 平台返回的单项失败；
- 批量实现后遗留的字段或接口问题；
- Token 不足时的最后收口。

## 6. 与第一次经历的关键对照

| 维度 | 第一次经历 | 第二次经历 |
|---|---|---|
| Agent 能力 | 较弱，反复修复仍不能收敛 | 较强，能完成大部分开发任务 |
| 主要风险 | 做不对、重复错误、无法验证修复 | 目标偏移、范围越界、执行过多 |
| 测试循环 | 约 80% 时间耗在失败—修复循环 | 跳过完整 TDD 后，用审查和定点修复收口 |
| 状态文档风险 | 摘要可能遗漏 README 需求 | 状态文档可能误认样本角色并放大目标偏移 |
| 新会话作用 | handoff 后仍重复错误 | 新审查会话有效发现批量实现问题 |
| 最终结果 | 未推进到完整业务验收 | 大部分业务实例通过 |

两次经历共同说明：状态文档、会话切分和测试流程都不是目的。它们应根据 Agent 能力和资源限制进行调整，但三个底线仍然存在：

1. README 与当前文件的事实不能被二手文档替代；
2. 每次实现必须有清楚的范围和停止点；
3. 无论采用逐切片还是快速批量路线，最后都必须通过真实测试、独立审查和定点验证收敛。
